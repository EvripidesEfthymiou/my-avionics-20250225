import { ComponentProps, DisplayComponent, Subscribable, SubscribableSet, ToggleableClassNameRecord, Unit, UnitFamily, VNode } from '@microsoft/msfs-sdk';
import './WeightBalancePaneViewLoadStation.css';
/**
 * Component props for {@link WeightBalancePaneViewLoadStation}.
 */
export interface WeightBalancePaneViewLoadStationProps extends ComponentProps {
    /** Whether to show the display. */
    show: Subscribable<boolean>;
    /** The display's label. */
    label: string;
    /** The load weight of the display's station, in pounds. */
    loadWeight: Subscribable<number>;
    /** The unit type in which to display the station's load weight. */
    weightDisplayUnit: Subscribable<Unit<UnitFamily.Weight>>;
    /** CSS class(es) to apply to the display's root element. */
    class?: string | SubscribableSet<string> | ToggleableClassNameRecord;
}
/**
 * A weight and balance pane load station display.
 */
export declare class WeightBalancePaneViewLoadStation extends DisplayComponent<WeightBalancePaneViewLoadStationProps> {
    private static readonly RESERVED_CLASSES;
    private static readonly WEIGHT_FORMATTER;
    private readonly rootCssClass;
    private readonly weightRef;
    private readonly weightValue;
    private readonly subscriptions;
    /** @inheritDoc */
    onAfterRender(): void;
    /** @inheritDoc */
    render(): VNode;
    /** @inheritDoc */
    destroy(): void;
}
//# sourceMappingURL=WeightBalancePaneViewLoadStation.d.ts.map