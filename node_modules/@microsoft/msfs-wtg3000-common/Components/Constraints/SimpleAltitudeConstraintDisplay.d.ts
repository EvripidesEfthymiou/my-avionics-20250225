import { ComponentProps, DisplayComponent, NumberUnitInterface, Subscribable, UnitFamily, VNode } from '@microsoft/msfs-sdk';
import './SimpleAltitudeConstraintDisplay.css';
/** The props for {@link SimpleAltitudeConstraintDisplay}. */
export interface SimpleAltitudeConstraintDisplayProps extends ComponentProps {
    /** Altitude 1. */
    altitude1: Subscribable<NumberUnitInterface<UnitFamily.Distance>>;
    /** Whether the altitude 1 should be displayed as a flight level. */
    displayAltitude1AsFlightLevel: Subscribable<boolean>;
}
/** Displays an altitude constraint, simply. */
export declare class SimpleAltitudeConstraintDisplay extends DisplayComponent<SimpleAltitudeConstraintDisplayProps> {
    private readonly altitude1Feet;
    private readonly line1Content;
    private readonly classList;
    private readonly subs;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=SimpleAltitudeConstraintDisplay.d.ts.map