import { ComponentProps, DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import { Fms, VNavDataProvider } from '@microsoft/msfs-garminsdk';
import { G3000FlightPlannerId } from '../../../CommonTypes';
import { FlightPlanStore } from '../../../FlightPlan/FlightPlanStore';
import './CurrentVnavProfilePanel.css';
/**
 * The properties for the {@link CurrentVnavProfilePanel} component.
 */
export interface CurrentVnavProfilePanelProps extends ComponentProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** The Fms. */
    fms: Fms<G3000FlightPlannerId>;
    /** Which flight plan index to handle events for. */
    planIndex: number;
    /** The flight plan store to use. */
    store: FlightPlanStore;
    /** A provider of VNAV data. */
    vnavDataProvider: VNavDataProvider;
}
/**
 * A panel which displays information related to the current VNAV profile.
 */
export declare class CurrentVnavProfilePanel extends DisplayComponent<CurrentVnavProfilePanelProps> {
    private thisNode?;
    private readonly activeWaypointCssClass;
    private readonly cruiseAltitudeCssClass;
    private readonly unitsSettingManager;
    private readonly vnavProfileStore;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** Resumes the vnav panel. */
    resume(): void;
    /** Pauses the vnav panel. */
    pause(): void;
    /** @inheritdoc */
    render(): VNode;
    /** Destroys subs and comps. */
    destroy(): void;
}
//# sourceMappingURL=CurrentVnavProfilePanel.d.ts.map