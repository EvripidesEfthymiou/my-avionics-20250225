import { ComponentProps, DisplayComponent, EventBus, Subscribable, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { DateTimeUserSettingTypes, UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { FlightPlanStore } from '../../../FlightPlan/FlightPlanStore';
import { FlightPlanTextData, FlightPlanTextRowData } from '../../../FlightPlan/FlightPlanTextUpdater';
import './FlightPlanTextRow.css';
/**
 * The properties for the {@link FlightPlanTextRow} component.
 */
export interface FlightPlanTextRowProps extends ComponentProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** The row index. */
    index: number;
    /** The units settings manager. */
    unitsSettingManager: UnitsUserSettingManager;
    /** The date time user settings. */
    dateTimeSettingManager: UserSettingManager<DateTimeUserSettingTypes>;
    /** The flight plan text data. */
    data: Subscribable<FlightPlanTextData | undefined>;
    /** The selected row. */
    selectedRow: Subscribable<FlightPlanTextRowData | undefined>;
    /** The flight plan store. */
    store: FlightPlanStore;
    /** Whether to use cumulative distance or not. */
    mapInsetTextCumulativeSetting: Subscribable<boolean>;
}
/**
 * A single row in a flight plan text panel. Displays information on a single flight plan leg or a header describing a
 * flight plan segment.
 */
export declare class FlightPlanTextRow extends DisplayComponent<FlightPlanTextRowProps> {
    private readonly rowData;
    private readonly isSelected;
    private readonly name;
    private readonly segmentListData;
    private readonly segmentListDataPipes;
    private readonly approach;
    private readonly approachAirport;
    private readonly approachPrefix;
    private readonly legListData;
    private readonly legDefinition;
    private readonly isAirwayLeg;
    private readonly isActiveLeg;
    private readonly isHeadingLeg;
    private readonly isHoldLeg;
    private readonly isHoldTimeLeg;
    private readonly isFirstLegInPlan;
    private readonly isDmeArcLeg;
    private readonly fuelRemaining;
    private readonly estimatedTimeEnroute;
    private readonly estimatedTimeEnrouteCumulative;
    private readonly eteFinal;
    private readonly estimatedTimeOfArrival;
    private readonly airwayExitText;
    private readonly legListDataPipes;
    private readonly isAltitudeCyan;
    private readonly altDescDisplay;
    private readonly isAltitudeEditedDisplay;
    private readonly isAltitudeInvalidDisplay;
    private readonly altitude1Display;
    private readonly altitude2Display;
    private readonly displayAltitude1AsFlightLevelDisplay;
    private readonly displayAltitude2AsFlightLevelDisplay;
    private readonly isAltitudeVisible;
    private readonly legSuffix;
    private readonly displayDtk;
    private readonly dtkString;
    private readonly distanceString;
    private readonly displayDistance;
    private readonly distanceCumulative;
    private readonly distanceFinal;
    private readonly cumulativePipes;
    private readonly timeFormat;
    private readonly classList;
    private readonly subs;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Resets this row's segment data.
     */
    private resetSegmentData;
    /**
     * Resets this row's leg data.
     */
    private resetLegData;
    /** @inheritdoc */
    render(): VNode;
    /** Destroys subs and comps. */
    destroy(): void;
}
//# sourceMappingURL=FlightPlanTextRow.d.ts.map