import { ResolvableConfig } from '../Config/Config';
import { G3000ChecklistSetDef } from './G3000ChecklistDefinition';
/**
 * A configuration object which defines checklist options.
 */
export declare class ChecklistConfig implements ResolvableConfig<(timeout: number) => Promise<G3000ChecklistSetDef | undefined>> {
    /** @inheritDoc */
    readonly isResolvableConfig = true;
    /** The URL of the file from which to parse the checklist definition. */
    readonly checklistFileURL?: string;
    /**
     * Creates a new ChecklistConfig from a configuration document element.
     * @param element A configuration document element.
     */
    constructor(element: Element | undefined);
    /** @inheritDoc */
    resolve(): (timeout: number) => Promise<G3000ChecklistSetDef | undefined>;
}
//# sourceMappingURL=ChecklistConfig.d.ts.map