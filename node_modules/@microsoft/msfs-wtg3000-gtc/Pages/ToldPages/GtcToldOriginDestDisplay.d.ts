import { ComponentProps, DisplayComponent, OneWayRunway, Subscribable, Unit, UnitFamily, VNode } from '@microsoft/msfs-sdk';
import { AirportWaypoint } from '@microsoft/msfs-garminsdk';
import './GtcToldOriginDestDisplay.css';
/**
 * Component props for GtcToldOriginDestDisplay.
 */
export interface GtcToldOriginDestDisplayProps extends ComponentProps {
    /** The selected airport. */
    selectedAirport: Subscribable<AirportWaypoint | null>;
    /** The selected runway. */
    selectedRunway: Subscribable<OneWayRunway | null>;
    /** The unit type in which to display the selected runway length. */
    runwayLengthDisplayUnit: Subscribable<Unit<UnitFamily.Distance>>;
    /** Whether to include the displaced threshold in the length of the runway. */
    includeDisplacedThreshold: boolean;
}
/**
 * Displays information about a GTC TOLD (takeoff/landing) page origin/destination, including the selected airport,
 * runway, and usable runway length.
 */
export declare class GtcToldOriginDestDisplay extends DisplayComponent<GtcToldOriginDestDisplayProps> {
    private static readonly LENGTH_FORMATTER;
    private readonly waypointRef;
    private readonly runwayLengthRef;
    private readonly runwayLengthCssClass;
    private readonly runwayLengthNullCssClass;
    private readonly runwayText;
    private readonly runwayLength;
    private runwaySub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcToldOriginDestDisplay.d.ts.map