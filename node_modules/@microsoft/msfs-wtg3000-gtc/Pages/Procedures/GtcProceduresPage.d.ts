import { VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { FlightPlanStore } from '@microsoft/msfs-wtg3000-common';
import { GtcView, GtcViewProps } from '../../GtcService/GtcView';
import './GtcProceduresPage.css';
/**
 * The properties for the GtcProceduresPage component.
 */
interface GtcProceduresPageProps extends GtcViewProps {
    /** An instance of the Fms. */
    fms: Fms;
    /** Which flight plan index to work with. */
    planIndex: number;
    /** The flight plan store. */
    store: FlightPlanStore;
}
/**
 * Displays the loaded procedures and links to the procedure pages.
 */
export declare class GtcProceduresPage extends GtcView<GtcProceduresPageProps> {
    private thisNode?;
    private readonly departureText;
    private readonly arrivalText;
    private readonly canActivateApproach;
    private readonly canActivateVtf;
    private readonly canMissedApproachActivate;
    private clockSub?;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    onResume(): void;
    /** @inheritdoc */
    onPause(): void;
    /** Runs checks to see if certain buttons should be enabled or not. */
    private update;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=GtcProceduresPage.d.ts.map