import { VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { GtcDialogResult, GtcDialogView } from '../../Dialog/GtcDialogView';
import { GtcView, GtcViewProps } from '../../GtcService/GtcView';
import { GtcInteractionEvent } from '../../GtcService';
import { HoldInfo, HoldInput } from './HoldStore';
import './GtcHoldPage.css';
/**
 * A request input for {@link GtcHoldPage}.
 */
export interface GtcHoldPageInput extends HoldInput {
    /** The GTC view title to display with the page. Defaults to `'Hold at Waypoint'` */
    title?: string;
}
/**
 * A request result returned by {@link GtcHoldPage}.
 */
export type GtcHoldPageOutput = HoldInfo | 'cancel-hold';
/** The properties for the {@link GtcHoldPage} component. */
export interface GtcHoldPageProps extends GtcViewProps {
    /** The FMS. */
    fms: Fms;
}
/**
 * A GTC hold page.
 */
export declare class GtcHoldPage extends GtcView<GtcHoldPageProps> implements GtcDialogView<GtcHoldPageInput, GtcHoldPageOutput> {
    private thisNode?;
    private readonly unitsSettingManager;
    private readonly store;
    private readonly controller;
    readonly legName: import("@microsoft/msfs-sdk").MappedSubscribable<string>;
    private readonly holdDescription;
    private readonly cancelButtonLabel;
    private readonly showOnMap;
    private resolveFunction?;
    private resultObject;
    private isAlive;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    request(input: GtcHoldPageInput): Promise<GtcDialogResult<GtcHoldPageOutput>>;
    /** @inheritdoc */
    onClose(): void;
    /** @inheritdoc */
    onGtcInteractionEvent(event: GtcInteractionEvent): boolean;
    /**
     * Clears this dialog's pending request and fulfills the pending request Promise if one exists.
     */
    private cleanupRequest;
    /**
     * Attempts to resolve the current request.
     */
    private resolve;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcHoldPage.d.ts.map