import { ComponentProps, DisplayComponent, Subscribable, VNode } from '@microsoft/msfs-sdk';
import { HorizonDirectorCueOption } from '@microsoft/msfs-wtg3000-common';
import { GtcInteractionEvent, GtcInteractionHandler } from '../../GtcService/GtcInteractionEvent';
import { GtcService } from '../../GtcService/GtcService';
import { SidebarState } from '../../GtcService/Sidebar';
import './GtcPfdSettingsPageMasterList.css';
/**
 * Component props for GtcPfdSettingsPageMasterList.
 */
export interface GtcPfdSettingsPageMasterListProps extends ComponentProps {
    /** The GTC service. */
    gtcService: GtcService;
    /** PFD horizon display flight director cue style support. */
    horizonDirectorCueOption: HorizonDirectorCueOption;
    /** The height of each list item, in pixels. */
    listItemHeight: number;
    /** The spacing between each list item, in pixels. */
    listItemSpacing: number;
    /** The SidebarState to use. */
    sidebarState?: SidebarState | Subscribable<SidebarState | null>;
}
/**
 * A GTC PFD setting page master settings list.
 */
export declare class GtcPfdSettingsPageMasterList extends DisplayComponent<GtcPfdSettingsPageMasterListProps> implements GtcInteractionHandler {
    private readonly listRef;
    private readonly pfdSettingManager;
    private readonly dateTimeSettingManager;
    private readonly comRadioSettingManager;
    private readonly headingLabelShowSetting;
    private readonly horizonHeadingButtonState;
    private readonly isLocalOffsetEditable;
    private readonly localOffset;
    private localOffsetPipe?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    onGtcInteractionEvent(event: GtcInteractionEvent): boolean;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcPfdSettingsPageMasterList.d.ts.map