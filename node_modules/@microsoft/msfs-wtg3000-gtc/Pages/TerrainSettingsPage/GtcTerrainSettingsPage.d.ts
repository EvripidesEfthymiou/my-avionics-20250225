import { VNode } from '@microsoft/msfs-sdk';
import { TerrainSystemStateDataProvider } from '@microsoft/msfs-garminsdk';
import { TerrainSystemConfig } from '@microsoft/msfs-wtg3000-common';
import { GtcView, GtcViewProps } from '../../GtcService/GtcView';
import './GtcTerrainSettingsPage.css';
/**
 * Component props for {@link GtcTerrainSettingsPage}.
 */
export interface GtcTerrainSettingsPageProps extends GtcViewProps {
    /** A terrain system config object. */
    terrainConfig: TerrainSystemConfig;
    /** A provider of terrain system state data. */
    terrainSystemStateDataProvider: TerrainSystemStateDataProvider;
}
/**
 * A GTC terrain settings page.
 */
export declare class GtcTerrainSettingsPage extends GtcView<GtcTerrainSettingsPageProps> {
    private readonly settingsDisplay;
    private readonly aviationDataButton;
    private readonly displayPaneIndex;
    /**
     * Creates a new instance of GtcTerrainSettingsPage.
     * @param props The component's props.
     * @throws Error if a display pane index is not defined for this view.
     */
    constructor(props: GtcTerrainSettingsPageProps);
    /** @inheritDoc */
    onAfterRender(): void;
    /** @inheritDoc */
    render(): VNode | null;
    /** @inheritDoc */
    destroy(): void;
}
//# sourceMappingURL=GtcTerrainSettingsPage.d.ts.map