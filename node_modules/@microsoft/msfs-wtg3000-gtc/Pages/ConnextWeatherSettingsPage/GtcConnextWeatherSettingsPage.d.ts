import { VNode } from '@microsoft/msfs-sdk';
import { GtcView, GtcViewProps } from '../../GtcService/GtcView';
import './GtcConnextWeatherSettingsPage.css';
/**
 * A GTC Connext weather settings page.
 */
export declare class GtcConnextWeatherSettingsPage extends GtcView {
    private thisNode?;
    private readonly listRef;
    private readonly displayPaneIndex;
    private readonly mapSettingManager;
    private readonly listItemHeight;
    private readonly listItemSpacing;
    /**
     * Constructor.
     * @param props This component's props.
     * @throws Error if a display pane index is not defined for this view.
     */
    constructor(props: GtcViewProps);
    /** @inheritDoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    render(): VNode;
    /**
     * Renders this page's data coverage popup.
     * @param gtcService The GTC service.
     * @param controlMode The control mode to which the popup belongs.
     * @param displayPaneIndex The index of the display pane associated with the popup.
     * @returns This page's data coverage popup, as a VNode.
     */
    private renderDataCoveragePopup;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcConnextWeatherSettingsPage.d.ts.map