import { DisplayComponent, Subscribable, UserSetting, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { GtcTouchButtonProps } from '../../Components/TouchButton/GtcTouchButton';
import { GtcService } from '../../GtcService/GtcService';
import './GtcAdvancedVnavSpeedButton.css';
import './GtcAdvancedVnavTerminalLimitButton.css';
/**
 * Component props for GtcAdvancedVnavTerminalLimitButton.
 */
export interface GtcAdvancedVnavTerminalLimitButtonProps extends Omit<GtcTouchButtonProps, 'label' | 'onPressed' | 'isHighlighted' | 'gtcOrientation'> {
    /** The GTC service. */
    gtcService: GtcService;
    /** The user setting for the terminal area speed limit controlled by the button. */
    speedLimitSetting: UserSetting<number>;
    /** The user setting for the terminal area speed limit ceiling controlled by the button. */
    ceilingSetting: UserSetting<number>;
    /** The user setting for the terminal area speed limit radius controlled by the button. */
    radiusSetting: UserSetting<number>;
    /** The  */
    ident: Subscribable<string | undefined>;
    /** A manager for display unit user settings. */
    unitSettingManager: UnitsUserSettingManager;
}
/**
 * A GTC touchscreen button which displays a VNAV terminal area speed limit and when pressed, opens a chain of dialogs
 * to allow the user to change the settings for the speed limit.
 */
export declare class GtcAdvancedVnavTerminalLimitButton extends DisplayComponent<GtcAdvancedVnavTerminalLimitButtonProps> {
    private static readonly ROOT_CSS_CLASSES;
    private static readonly RESERVED_CSS_CLASSES;
    private static readonly SPEED_FORMATTER;
    private static readonly ALTITUDE_FORMATTER;
    private static readonly DISTANCE_FORMATTER;
    private static readonly MAXIMUM_FEET;
    private static readonly MAXIMUM_METERS;
    private static readonly MINIMUM_NM;
    private static readonly MAXIMUM_NM;
    private static readonly MINIMUM_KM;
    private static readonly MAXIMUM_KM;
    private readonly speedLimitRef;
    private readonly ceilingRef;
    private readonly radiusRef;
    private readonly dialogChainSteps;
    private readonly speedLimitValue;
    private readonly ceilingValue;
    private readonly radiusValue;
    private readonly identText;
    private cssClassSub?;
    private speedLimitPipe?;
    private ceilingPipe?;
    private radiusPipe?;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Responds to when this button is pressed.
     */
    private onPressed;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcAdvancedVnavTerminalLimitButton.d.ts.map