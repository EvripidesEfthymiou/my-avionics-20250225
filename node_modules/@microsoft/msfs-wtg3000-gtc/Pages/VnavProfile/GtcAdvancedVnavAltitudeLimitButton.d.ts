import { DisplayComponent, UserSetting, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { GtcTouchButtonProps } from '../../Components/TouchButton/GtcTouchButton';
import { GtcService } from '../../GtcService/GtcService';
import './GtcAdvancedVnavSpeedButton.css';
import './GtcAdvancedVnavAltitudeLimitButton.css';
/**
 * Component props for GtcAdvancedVnavAltitudeLimitButton.
 */
export interface GtcAdvancedVnavAltitudeLimitButtonProps extends Omit<GtcTouchButtonProps, 'label' | 'onPressed' | 'isHighlighted' | 'gtcOrientation'> {
    /** The GTC service. */
    gtcService: GtcService;
    /** The user setting for the altitude speed limit controlled by the button. */
    speedLimitSetting: UserSetting<number>;
    /** The user setting for the altitude speed limit ceiling controlled by the button. */
    ceilingSetting: UserSetting<number>;
    /** A manager for display unit user settings. */
    unitSettingManager: UnitsUserSettingManager;
}
/**
 * A GTC touchscreen button which displays a VNAV altitude speed limit and when pressed, opens a chain of dialogs to
 * allow the user to change the settings for the speed limit.
 */
export declare class GtcAdvancedVnavAltitudeLimitButton extends DisplayComponent<GtcAdvancedVnavAltitudeLimitButtonProps> {
    private static readonly ROOT_CSS_CLASSES;
    private static readonly RESERVED_CSS_CLASSES;
    private static readonly SPEED_FORMATTER;
    private static readonly ALTITUDE_FORMATTER;
    private static readonly MAXIMUM_FEET;
    private static readonly MAXIMUM_METERS;
    private readonly speedLimitRef;
    private readonly ceilingRef;
    private readonly dialogChainSteps;
    private readonly speedLimitValue;
    private readonly ceilingValue;
    private cssClassSub?;
    private speedLimitPipe?;
    private ceilingPipe?;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Responds to when this button is pressed.
     */
    private onPressed;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcAdvancedVnavAltitudeLimitButton.d.ts.map