import { DisplayComponent, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { GtcTouchButtonProps } from '../../Components/TouchButton/GtcTouchButton';
import { GtcService } from '../../GtcService/GtcService';
import './GtcAdvancedVnavSpeedButton.css';
import './GtcAdvancedVnavTransitionButton.css';
/**
 * Component props for GtcAdvancedVnavTransitionButton.
 */
export interface GtcAdvancedVnavTransitionButtonProps extends Omit<GtcTouchButtonProps, 'label' | 'onPressed' | 'gtcOrientation'> {
    /** The GTC service. */
    gtcService: GtcService;
    /** A manager for display unit user settings. */
    unitSettingManager: UnitsUserSettingManager;
}
/**
 * A GTC touchscreen button which displays a VNAV transition altitude and when pressed, opens a dialog to allow the
 * user to change the transition altitude.
 */
export declare class GtcAdvancedVnavTransitionButton extends DisplayComponent<GtcAdvancedVnavTransitionButtonProps> {
    private static readonly ROOT_CSS_CLASSES;
    private static readonly RESERVED_CSS_CLASSES;
    private static readonly FORMATTER;
    private static readonly MAXIMUM_FEET;
    private static readonly MAXIMUM_METERS;
    private readonly speedLimitRef;
    private readonly ceilingRef;
    private readonly value;
    private cssClassSub?;
    /**
     * Responds to when this button is pressed.
     */
    private onPressed;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcAdvancedVnavTransitionButton.d.ts.map