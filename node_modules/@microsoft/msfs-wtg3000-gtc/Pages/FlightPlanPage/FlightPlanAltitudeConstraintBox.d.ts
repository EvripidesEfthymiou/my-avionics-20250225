import { ComponentProps, DisplayComponent, VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { FlightPlanLegData } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import './FlightPlanAltitudeConstraintBox.css';
/** The props for FlightPlanAltitudeConstraintBox. */
interface FlightPlanAltitudeConstraintBoxProps extends ComponentProps {
    /** The leg list item data. */
    legData: FlightPlanLegData;
    /** The GtcService. */
    gtcService: GtcService;
    /** The FMS. */
    fms: Fms;
    /** The flight plan index. */
    planIndex: number;
}
/** A component that wraps list item content for use with GtcList. */
export declare class FlightPlanAltitudeConstraintBox extends DisplayComponent<FlightPlanAltitudeConstraintBoxProps> {
    private readonly touchButtonRef;
    private readonly altitudeDisplayRef;
    private readonly subs;
    private readonly classList;
    private readonly publishedAltitudeFeet1;
    /** @inheritdoc */
    onAfterRender(): void;
    /** Handles the button being pressed in simple mode. */
    private handlePressedSimpleVnav;
    /** Handles the button being pressed in advanced mode. */
    private handlePressedAdvancedVnav;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanAltitudeConstraintBox.d.ts.map