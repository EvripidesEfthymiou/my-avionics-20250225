import { VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { FlightPlanStore, FlightPlanLegData } from '@microsoft/msfs-wtg3000-common';
import { GtcViewProps, GtcView } from '../../GtcService/GtcView';
import './FlightPlanFpaSpeedSlideoutMenu.css';
/** The properties for the {@link FlightPlanFpaSpeedSlideoutMenu} component. */
export interface FlightPlanFpaSpeedSlideoutMenuProps extends GtcViewProps {
    /** An instance of the Fms. */
    fms: Fms;
    /** The flight plan store. */
    store: FlightPlanStore;
}
/** Displays the loaded procedures and links to the procedure pages. */
export declare class FlightPlanFpaSpeedSlideoutMenu extends GtcView<FlightPlanFpaSpeedSlideoutMenuProps> {
    private thisNode?;
    private readonly unitsSettingManager;
    private readonly legListData;
    private readonly legFpa;
    private readonly isFpaEdited;
    private readonly speed;
    private readonly speedUnit;
    private readonly speedDesc;
    private readonly isAltitudeDesignated;
    private readonly isAltitudeEditable;
    private readonly altDesc;
    private readonly vnavPhase;
    private readonly displayFpa;
    private readonly pipes;
    private readonly showClimb;
    private readonly isFpaButtonEnabled;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /**
     * Pass initial leg list data to use.
     * @param legListData The leg list data.
     */
    request(legListData: FlightPlanLegData): void;
    /** @inheritdoc */
    onClose(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=FlightPlanFpaSpeedSlideoutMenu.d.ts.map