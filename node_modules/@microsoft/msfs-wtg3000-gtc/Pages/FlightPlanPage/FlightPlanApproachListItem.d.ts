import { ComponentProps, DisplayComponent, MutableSubscribable, VNode } from '@microsoft/msfs-sdk';
import { FlightPlanSegmentListData, FlightPlanStore, SelectableFlightPlanListData } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import './FlightPlanSegmentListItem.css';
/** The props for FlightPlanApproachListItem. */
interface FlightPlanApproachListItemProps extends ComponentProps {
    /** Data describing the list item's associated flight plan segment. */
    segmentListData: FlightPlanSegmentListData;
    /** The GtcService. */
    gtcService: GtcService;
    /** The flight plan store. */
    store: FlightPlanStore;
    /** A mutable subscribable which controls the selected list data. */
    selectedListData: MutableSubscribable<SelectableFlightPlanListData | null>;
}
/** The flight plan list item for an approach procedure. */
export declare class FlightPlanApproachListItem extends DisplayComponent<FlightPlanApproachListItemProps> {
    private readonly gtcListButtonRef;
    private readonly isSelected;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanApproachListItem.d.ts.map