import { ComponentProps, DisplayComponent, VNode, Subject, Subscribable } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { GtcService } from '../../GtcService/GtcService';
/** The props for AddEnrouteWaypointListItem. */
interface AddEnrouteWaypointListItemProps extends ComponentProps {
    /** The GtcService. */
    gtcService: GtcService;
    /** The FMS. */
    fms: Fms;
    /** The plan index to use. */
    planIndex: number;
    /** Whether this button is visible. Will be set to false when Done is clicked. */
    isVisible: Subject<boolean>;
    /** Whether the Done button should be enabled. */
    isDoneEnabled: Subscribable<boolean>;
}
/** Renders the Add Enroute Waypoint button for the flight plan list. */
export declare class AddEnrouteWaypointListItem extends DisplayComponent<AddEnrouteWaypointListItemProps> {
    private readonly addEnrouteWaypointButton;
    private readonly doneButton;
    private readonly onDoneButtonPressed;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=AddEnrouteWaypointListItem.d.ts.map