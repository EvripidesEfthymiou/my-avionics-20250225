import { ComponentProps, DisplayComponent, VNode, MutableSubscribable } from '@microsoft/msfs-sdk';
import { FlightPlanSegmentListData, SelectableFlightPlanListData } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import './FlightPlanSegmentListItem.css';
import './FlightPlanEnrouteListItem.css';
/** The props for FlightPlanSegmentListItem. */
interface FlightPlanEnrouteListItemProps extends ComponentProps {
    /** The GtcService. */
    gtcService: GtcService;
    /** The leg list item data. */
    segmentListData: FlightPlanSegmentListData;
    /** A mutable subscribable which controls the selected list data. */
    selectedListData: MutableSubscribable<SelectableFlightPlanListData | null>;
}
/** A component that wraps list item content for use with GtcList. */
export declare class FlightPlanEnrouteListItem extends DisplayComponent<FlightPlanEnrouteListItemProps> {
    private static readonly LIST_ITEM_CLASSES;
    private readonly gtcListButtonRef;
    private readonly listItemClasses;
    private readonly isSelected;
    private readonly labelText;
    private readonly subscriptions;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanEnrouteListItem.d.ts.map