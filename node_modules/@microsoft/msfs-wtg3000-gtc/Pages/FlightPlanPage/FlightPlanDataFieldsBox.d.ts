import { DisplayComponent, VNode, ComponentProps } from '@microsoft/msfs-sdk';
import { FlightPlanLegListData, FlightPlanStore } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import './FlightPlanDataFieldsBox.css';
/**
 * The properties for the {@link FlightPlanDataFieldsBox} component.
 */
interface FlightPlanDataFieldsBoxProps extends ComponentProps {
    /** The leg list data. */
    legListData: FlightPlanLegListData;
    /** The flight plan store. */
    store: FlightPlanStore;
    /** The GtcService. */
    gtcService: GtcService;
}
/**
 * The FlightPlanDataFieldsBox component.
 */
export declare class FlightPlanDataFieldsBox extends DisplayComponent<FlightPlanDataFieldsBoxProps> {
    private readonly dataField1;
    private readonly dataField2;
    private readonly classList;
    private readonly legData;
    private readonly isVisible;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanDataFieldsBox.d.ts.map