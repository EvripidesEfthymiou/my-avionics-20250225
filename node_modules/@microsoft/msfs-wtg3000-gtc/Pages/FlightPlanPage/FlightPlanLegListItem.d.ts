import { DisplayComponent, VNode, MutableSubscribable } from '@microsoft/msfs-sdk';
import { FlightPlanLegListData, FlightPlanStore, SelectableFlightPlanListData } from '@microsoft/msfs-wtg3000-common';
import { FlightPlanLegButtonProps } from './FlightPlanLegButton';
import './FlightPlanLegListItem.css';
/** The props for FlightPlanLegListItem. */
interface FlightPlanLegListItemProps extends FlightPlanLegButtonProps {
    /** Data describing the list item's associated flight plan leg. */
    legListData: FlightPlanLegListData;
    /** The flight plan index. */
    planIndex: number;
    /** The flight plan store. */
    store: FlightPlanStore;
    /**
     * A mutable subscribable which controls the selected list data. If not defined, pressing the list item's leg button
     * will not cause its list data to become selected or unselected.
     */
    selectedListData?: MutableSubscribable<SelectableFlightPlanListData | null>;
}
/** A component that wraps list item content for use with GtcList. */
export declare class FlightPlanLegListItem extends DisplayComponent<FlightPlanLegListItemProps> {
    private readonly listItemRef;
    private readonly legButtonRef;
    private readonly altitudeBoxRef;
    private readonly fpaSpeedBox;
    private readonly dataFieldsBoxRef;
    private readonly hideBorder;
    private readonly paddedListItem;
    private readonly classList;
    private readonly subs;
    private readonly airwaySubs;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode | null;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanLegListItem.d.ts.map