import { ComponentProps, DisplayComponent, VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { FlightPlanLegData } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import './FlightPlanFpaSpeedBox.css';
/** The props for FlightPlanFpaSpeedBox. */
interface FlightPlanFpaSpeedBoxProps extends ComponentProps {
    /** The leg list item data. */
    legData: FlightPlanLegData;
    /** The GtcService. */
    gtcService: GtcService;
    /** The FMS. */
    fms: Fms;
    /** The flight plan index. */
    planIndex: number;
}
/** A component that wraps list item content for use with GtcList. */
export declare class FlightPlanFpaSpeedBox extends DisplayComponent<FlightPlanFpaSpeedBoxProps> {
    private readonly touchButtonRef;
    private readonly fpaRef;
    private readonly speedRef;
    private readonly unitsSettingManager;
    private readonly fpa;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=FlightPlanFpaSpeedBox.d.ts.map