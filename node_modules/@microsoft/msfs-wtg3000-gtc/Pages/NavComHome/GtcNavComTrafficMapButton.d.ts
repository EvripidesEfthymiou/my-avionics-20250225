import { DisplayComponent, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { TouchButtonProps } from '@microsoft/msfs-garminsdk';
import { DisplayPaneSettings, PfdMapLayoutUserSettingTypes } from '@microsoft/msfs-wtg3000-common';
import './GtcNavComTrafficMapButton.css';
/**
 * Component props for GtcNavComTrafficMapButton.
 */
export interface GtcNavComTrafficMapButtonProps extends Omit<TouchButtonProps, 'label' | 'onPressed'> {
    /** The button's label. Defaults to `'Traffic Map'`. */
    label?: string;
    /** A manager for PFD map layout settings. */
    pfdMapLayoutSettingManager: UserSettingManager<PfdMapLayoutUserSettingTypes>;
    /** A manager for PFD display pane settings. */
    pfdDisplayPaneSettingManager: UserSettingManager<DisplayPaneSettings>;
}
/**
 * A touchscreen button which allows the user to activate the PFD traffic inset map, or the traffic map pane if the
 * PFD is in split mode.
 */
export declare class GtcNavComTrafficMapButton extends DisplayComponent<GtcNavComTrafficMapButtonProps> {
    private static readonly RESERVED_CSS_CLASSES;
    private readonly buttonRef;
    private readonly cssClassSet;
    private readonly pfdMapLayoutSetting;
    private readonly isTrafficInsetMapActive;
    private readonly isPfdInSplitMode;
    private readonly pfdPaneDesignatedViewSetting;
    private readonly pfdPaneViewSetting;
    private lastPfdNonTrafficInsetMapMode;
    private cssClassSub?;
    private mapLayoutSub?;
    private splitModeSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Responds to when this button is pressed.
     */
    private onPressed;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcNavComTrafficMapButton.d.ts.map