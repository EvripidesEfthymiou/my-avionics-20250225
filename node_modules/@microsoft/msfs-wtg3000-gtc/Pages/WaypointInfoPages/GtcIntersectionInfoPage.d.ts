import { FacilitySearchType, FacilityWaypoint, IcaoValue, IntersectionFacility, MutableSubscribable, VNode } from '@microsoft/msfs-sdk';
import { GtcWaypointInfoPage, GtcWaypointInfoPageProps } from './GtcWaypointInfoPage';
import './GtcIntersectionInfoPage.css';
/**
 * Component props for GtcIntersectionInfoPage.
 */
export interface GtcIntersectionInfoPageProps extends GtcWaypointInfoPageProps {
    /** A mutable subscribable from and to which to sync the page's selected intersection waypoint. */
    selectedIntersection: MutableSubscribable<FacilityWaypoint<IntersectionFacility> | null>;
}
/**
 * GTC view keys for popups owned by intersection information pages.
 */
declare enum GtcIntersectionInfoPagePopupKeys {
    Options = "IntersectionInfoOptions"
}
/**
 * A GTC intersection information page.
 */
export declare class GtcIntersectionInfoPage extends GtcWaypointInfoPage<FacilitySearchType.Intersection, GtcIntersectionInfoPageProps> {
    private static readonly VOR_ICON_KEY_MAP;
    private static readonly BEARING_FORMATTER;
    private static readonly DISTANCE_FORMATTER;
    protected readonly waypointSelectType = FacilitySearchType.Intersection;
    protected readonly optionsPopupKey = GtcIntersectionInfoPagePopupKeys.Options;
    private readonly infoRef;
    private readonly iconCache;
    private readonly nearestVorIdent;
    private readonly nearestVorIconSrc;
    private readonly nearestVorRadial;
    private readonly nearestVorDistance;
    private initSelectionOpId;
    private selectedIntPipeOut?;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Initializes this page's intersection selection.
     * @param facility The intersection facility to select, or its ICAO. If not defined, the selection will be
     * initialized to the most recently selected intersection.
     */
    initSelection(facility?: IntersectionFacility | IcaoValue): Promise<void>;
    /** @inheritdoc */
    onOpen(): void;
    /** @inheritdoc */
    onClose(): void;
    /** @inheritdoc */
    protected getCssClass(): string;
    /** @inheritdoc */
    protected renderContent(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=GtcIntersectionInfoPage.d.ts.map