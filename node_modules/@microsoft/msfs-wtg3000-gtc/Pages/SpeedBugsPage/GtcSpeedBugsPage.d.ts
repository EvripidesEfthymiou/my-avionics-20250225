import { UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { VSpeedUserSettingTypes } from '@microsoft/msfs-garminsdk';
import { VSpeedGroup, VSpeedGroupType } from '@microsoft/msfs-wtg3000-common';
import { GtcView, GtcViewProps } from '../../GtcService/GtcView';
import './GtcSpeedBugsPage.css';
/**
 * Component props for GtcSpeedBugsPage.
 */
export interface GtcSpeedBugsPageProps extends GtcViewProps {
    /** Definitions for the reference V-speeds to support, organized in groups. */
    vSpeedGroups: ReadonlyMap<VSpeedGroupType, VSpeedGroup>;
    /** A manager for reference V-speed user settings. */
    vSpeedSettingManager: UserSettingManager<VSpeedUserSettingTypes>;
    /** Whether TOLD (takeoff/landing) performance calculations are supported by the FMS. */
    isToldSupported: boolean;
}
/**
 * A GTC speed bugs page.
 */
export declare class GtcSpeedBugsPage extends GtcView<GtcSpeedBugsPageProps> {
    private static readonly TAB_LABEL_TEXT;
    private readonly tabContainerRef;
    private readonly restoreButtonRef;
    private readonly groupTypeTabPositions;
    private readonly allVSpeedNames;
    private readonly allUserValueSettings;
    private readonly allShowSettings;
    private readonly toldSettingManager;
    private readonly listItemHeight;
    private readonly listItemSpacing;
    private readonly isOnGround;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    onOpen(): void;
    /** @inheritdoc */
    onResume(): void;
    /** @inheritdoc */
    onPause(): void;
    /** @inheritdoc */
    render(): VNode;
    /**
     * Renders a settings tab for this page's tab container.
     * @param position The position of the tab.
     * @param vSpeedGroup The V-speed group associated with the tab.
     * @returns A settings tab for this page's tab container, as a VNode.
     */
    private renderTab;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcSpeedBugsPage.d.ts.map