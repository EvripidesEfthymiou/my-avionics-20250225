import { DisplayComponent, FacilityWaypoint, Subscribable, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { NearestWaypointEntry } from '@microsoft/msfs-wtg3000-common';
import { GtcService } from '../../GtcService/GtcService';
import { GtcListItemProps } from '../List/GtcListItem';
import './GtcNearestWaypointListItem.css';
/**
 * Component props for GtcNearestWaypointListItem.
 */
export interface GtcNearestWaypointListItemProps<EntryType extends NearestWaypointEntry<FacilityWaypoint<any>> = NearestWaypointEntry<FacilityWaypoint<any>>> extends GtcListItemProps {
    /** The GTC service. */
    gtcService: GtcService;
    /** Data pertaining to the list item's waypoint. */
    entry: EntryType;
    /** A manager for display unit settings. */
    unitsSettingManager: UnitsUserSettingManager;
    /** Whether the list item is selected. */
    isSelected?: Subscribable<boolean>;
    /** A callback function to execute when the list item's waypoint button is pressed. */
    onButtonPressed?: (entry: EntryType) => void;
}
/**
 * A GTC list item which displays information on a nearest waypoint entry. Renders a waypoint button, bearing and
 * distance to waypoint, and additional optional children.
 */
export declare class GtcNearestWaypointListItem<EntryType extends NearestWaypointEntry<FacilityWaypoint<any>> = NearestWaypointEntry<FacilityWaypoint<any>>> extends DisplayComponent<GtcNearestWaypointListItemProps<EntryType>> {
    private static readonly RESERVED_CSS_CLASSES;
    private static readonly BEARING_FORMATTER;
    private static readonly DISTANCE_FORMATTER;
    private readonly itemRef;
    private cssClassSub?;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcNearestWaypointListItem.d.ts.map