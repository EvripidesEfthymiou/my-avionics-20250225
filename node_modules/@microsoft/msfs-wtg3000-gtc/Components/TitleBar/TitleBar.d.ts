import { DisplayComponent, VNode, ComponentProps } from '@microsoft/msfs-sdk';
import { GtcService } from '../../GtcService/GtcService';
import './TitleBar.css';
/** The properties for the TitleBar component. */
interface TitleBarProps extends ComponentProps {
    /** The GtcService instance */
    gtcService: GtcService;
}
/** The TitleBar component. */
export declare class TitleBar extends DisplayComponent<TitleBarProps> {
    private readonly titleBarRef;
    private readonly titleInnerRef;
    private readonly title1Ref;
    private readonly title2Ref;
    private readonly title1;
    private readonly title2;
    private readonly minWidth;
    private readonly margin;
    private showTitle1;
    private readonly title;
    private titleSubscription?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
}
export {};
//# sourceMappingURL=TitleBar.d.ts.map