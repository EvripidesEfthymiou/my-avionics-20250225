import { ComponentProps, DisplayComponent, Subscribable, VNode } from '@microsoft/msfs-sdk';
import './GtcBearingArrow.css';
/**
 * Component props for GtcBearingArrow.
 */
export interface GtcBearingArrowProps extends ComponentProps {
    /** The relative bearing to display, in degrees. If the value is `NaN`, the arrow will be hidden. */
    relativeBearing: Subscribable<number>;
}
/**
 * A cyan arrow which rotates to point to a relative bearing.
 */
export declare class GtcBearingArrow extends DisplayComponent<GtcBearingArrowProps> {
    private readonly cssClass;
    private readonly cssTransform;
    private bearingSub?;
    /** @inheritDoc */
    onAfterRender(): void;
    /** @inheritDoc */
    render(): VNode;
    /** @inheritDoc */
    destroy(): void;
}
//# sourceMappingURL=GtcBearingArrow.d.ts.map