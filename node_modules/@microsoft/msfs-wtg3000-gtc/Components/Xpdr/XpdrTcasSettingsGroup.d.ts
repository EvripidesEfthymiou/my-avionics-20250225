import { ComponentProps, DisplayComponent, EventBus, SubscribableSet, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { TrafficUserSettingTypes } from '@microsoft/msfs-garminsdk';
import './XpdrTcasSettingsGroup.css';
/**
 * Component props for XpdrTcasSettingsGroup.
 */
export interface TcasXpdrSettingsGroupProps extends ComponentProps {
    /** The event bus. */
    bus: EventBus;
    /** A manager for traffic system user settings. */
    trafficSettingManager: UserSettingManager<TrafficUserSettingTypes>;
    /** CSS class(es) to apply to the group's root element. */
    class?: string | SubscribableSet<string>;
    /** Whether to show the long Altitude Reporting label */
    longAltReportingLabel?: boolean;
}
/**
 * A component which displays a group of buttons controlling transponder and TCAS-II operating modes.
 */
export declare class XpdrTcasSettingsGroup extends DisplayComponent<TcasXpdrSettingsGroupProps> {
    private static readonly ROOT_CLASSES;
    private readonly buttonRefs;
    private readonly controlPublisher;
    private readonly xpdrMode;
    private readonly trafficOperatingModeSetting;
    private readonly altState;
    private readonly onState;
    private readonly standbyState;
    private cssClassSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=XpdrTcasSettingsGroup.d.ts.map