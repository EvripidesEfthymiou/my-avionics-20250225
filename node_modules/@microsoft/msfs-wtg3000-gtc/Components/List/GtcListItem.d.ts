import { ComponentProps, DisplayComponent, VNode, SubscribableSet, Subscribable, ToggleableClassNameRecord } from '@microsoft/msfs-sdk';
import './GtcListItem.css';
/** The props for GtcListItem. */
export interface GtcListItemProps extends ComponentProps {
    /**
     * Hides the list item border, allowing the content to easily fill the entire space of the list-item.
     * Defaults to false.
     */
    hideBorder?: boolean | Subscribable<boolean>;
    /**
     * Adds some default, commonly used padding to the list item.
     * Defaults to false.
     */
    paddedListItem?: boolean | Subscribable<boolean>;
    /** A callback function to execute when the list item is destroyed. */
    onDestroy?: () => void;
    /** CSS class(es) to apply to the list item div element. */
    class?: string | SubscribableSet<string> | ToggleableClassNameRecord;
}
/** A component that wraps list item content for use with GtcList. */
export declare class GtcListItem extends DisplayComponent<GtcListItemProps> {
    private static readonly RESERVED_CLASSES;
    private static readonly BASE_CLASSES;
    private readonly hideBorder;
    private readonly paddedListItem;
    private thisNode?;
    private readonly subscriptions;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcListItem.d.ts.map