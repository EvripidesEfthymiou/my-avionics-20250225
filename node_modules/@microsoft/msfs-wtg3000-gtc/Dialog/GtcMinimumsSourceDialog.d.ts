import { MinimumsMode, VNode } from '@microsoft/msfs-sdk';
import { GtcDialogResult, GtcDialogView } from './GtcDialogView';
import { GtcView, GtcViewProps } from '../GtcService/GtcView';
import './GtcMinimumsSourceDialog.css';
/**
 * Component props for GtcMinimumsSourceDialog.
 */
export interface GtcMinimumsSourceDialogProps extends GtcViewProps {
    /** Whether to support radar minimums. */
    supportRadarMins: boolean;
}
/**
 * A pop-up dialog which allows the user to select a minimums mode.
 */
export declare class GtcMinimumsSourceDialog extends GtcView<GtcMinimumsSourceDialogProps> implements GtcDialogView<MinimumsMode, MinimumsMode> {
    private readonly offRef;
    private readonly baroRef;
    private readonly tempCompRef;
    private readonly radarRef;
    private readonly isOffHighlighted;
    private readonly isBaroHighlighted;
    private readonly isTempCompHighlighted;
    private readonly isRadarHighlighted;
    private resolveFunction?;
    private resultObject;
    private isAlive;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    request(initialMode: MinimumsMode): Promise<GtcDialogResult<MinimumsMode>>;
    /** @inheritDoc */
    onClose(): void;
    /**
     * Responds to when a minimums mode is selected.
     * @param mode The selected minimums mode.
     */
    private onSelected;
    /**
     * Clears this dialog's pending request and fulfills the pending request Promise if one exists.
     */
    private cleanupRequest;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=GtcMinimumsSourceDialog.d.ts.map