import { ComponentProps, DisplayComponent, VNode } from '@microsoft/msfs-sdk';
import { AvionicsConfig } from '@microsoft/msfs-wtg3000-common';
import { LabelBarPluginHandlers } from '../Components/LabelBar/LabelBar';
import { GtcService } from './GtcService';
import './GtcContainer.css';
/** The props for GtcContainer. */
export interface GtcContainerProps extends ComponentProps {
    /** The GtcService instance. */
    gtcService: GtcService;
    /** The instrument's general configuration object. */
    config: AvionicsConfig;
    /**
     * An array of plugin label bar handlers. The array should be ordered such that the handlers appear in the order in
     * which their parent plugins were loaded.
     */
    pluginLabelBarHandlers: readonly Readonly<LabelBarPluginHandlers>[];
}
/** Top level GTC component. */
export declare class GtcContainer extends DisplayComponent<GtcContainerProps> {
    private readonly gtcMainRef;
    private readonly containerRefs;
    private readonly noPaneSelectedRef;
    private readonly navComHeaderRef;
    /** @inheritdoc*/
    onAfterRender(): void;
    /**
     * Stops propogation of mouse events that were not for the left mouse button.
     * Mainly prevents interacting with the GTC with right mouse button clicks.
     * @param event The mouse event.
     */
    private readonly filterMouseEvent;
    private readonly updateNoSelectedPaneOverlay;
    /** @inheritdoc*/
    render(): VNode;
}
//# sourceMappingURL=GtcContainer.d.ts.map