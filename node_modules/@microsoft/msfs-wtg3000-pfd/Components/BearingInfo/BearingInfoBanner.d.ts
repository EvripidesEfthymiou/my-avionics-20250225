import { ComponentProps, DisplayComponent, EventBus, Subscribable, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { G3000NavIndicators } from '@microsoft/msfs-wtg3000-common';
import './BearingInfoBanner.css';
/**
 * Component props for BearingInfoBanner.
 */
export interface BearingInfoBannerProps extends ComponentProps {
    /** The event bus. */
    bus: EventBus;
    /** The number of supported ADF radios. */
    adfRadioCount: 0 | 1 | 2;
    /** The nav indicator collection for the PFD to which the display belongs. */
    navIndicators: G3000NavIndicators;
    /** A manager for display unit user settings. */
    unitsSettingManager: UnitsUserSettingManager;
    /** Whether the HSI map is enabled. */
    isHsiMapEnabled: Subscribable<boolean>;
    /** Whether the display should be decluttered. */
    declutter: Subscribable<boolean>;
    /** Whether the display's parent PFD includes the softkey menu. */
    softkey: boolean;
}
/**
 * A G3000 bearing info display banner.
 */
export declare class BearingInfoBanner extends DisplayComponent<BearingInfoBannerProps> {
    private static readonly BACKGROUND_STYLES;
    private readonly info1Ref;
    private readonly info2Ref;
    private readonly rootCssClass;
    private readonly backgroundStyles;
    private readonly backgroundPath;
    private readonly isVisible;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=BearingInfoBanner.d.ts.map