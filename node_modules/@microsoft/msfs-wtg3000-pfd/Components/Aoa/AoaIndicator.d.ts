import { ComponentProps, DisplayComponent, EventBus, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { AoaDataProvider } from '@microsoft/msfs-garminsdk';
import { AoaIndicatorUserSettingTypes } from '@microsoft/msfs-wtg3000-common';
import { AoaIndicatorConfig } from './AoaIndicatorConfig';
import './AoaIndicator.css';
/**
 * Component props for {@link AoaIndicator}.
 */
export interface AoaIndicatorProps extends ComponentProps {
    /** The event bus. */
    bus: EventBus;
    /** The configuration object for the indicator. */
    config: AoaIndicatorConfig;
    /** A data provider for the indicator. */
    dataProvider: AoaDataProvider;
    /** A manager for angle of attack indicator user settings. */
    settingManager: UserSettingManager<AoaIndicatorUserSettingTypes>;
}
/**
 * A G3000 PFD angle of attack indicator.
 */
export declare class AoaIndicator extends DisplayComponent<AoaIndicatorProps> {
    private readonly rootRef;
    private readonly gear1Position;
    private readonly gear2Position;
    private readonly gear3Position;
    private readonly flapsLeftAngle;
    private readonly flapsRightAngle;
    private readonly declutter;
    private readonly subscriptions;
    private readonly aoaDefs;
    /** @inheritDoc */
    onAfterRender(): void;
    /** @inheritDoc */
    render(): VNode;
    /** @inheritDoc */
    destroy(): void;
}
//# sourceMappingURL=AoaIndicator.d.ts.map