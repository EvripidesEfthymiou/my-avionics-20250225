import { ComponentProps, DisplayComponent, Subscribable, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager, WindDataProvider } from '@microsoft/msfs-garminsdk';
import { WindDisplayUserSettingTypes } from '@microsoft/msfs-wtg3000-common';
import './WindDisplay.css';
/**
 * Component props for WindDisplay.
 */
export interface WindDisplayProps extends ComponentProps {
    /** A provider of wind data. */
    dataProvider: WindDataProvider;
    /** A manager for wind display user settings. */
    windDisplaySettingManager: UserSettingManager<WindDisplayUserSettingTypes>;
    /** A manager for display units user settings. */
    unitsSettingManager: UnitsUserSettingManager;
    /** Whether the display should be decluttered. */
    declutter: Subscribable<boolean>;
}
/**
 * A G3000 PFD wind display.
 */
export declare class WindDisplay extends DisplayComponent<WindDisplayProps> {
    private static readonly MODE_MAP;
    private readonly ref;
    private readonly declutter;
    private readonly option;
    private displayModeSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=WindDisplay.d.ts.map