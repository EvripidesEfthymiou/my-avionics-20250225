import { ComponentProps, DisplayComponent, Subscribable, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { DmeUserSettingTypes, UnitsUserSettingManager } from '@microsoft/msfs-garminsdk';
import { G3000DmeInfoNavIndicator, G3000NavInfoNavIndicator } from '@microsoft/msfs-wtg3000-common';
import './NavDmeInfo.css';
/**
 * Component props for NavDmeInfo.
 */
export interface NavDmeInfoProps extends ComponentProps {
    /** The nav info nav indicator. */
    navInfoIndicator: G3000NavInfoNavIndicator;
    /** The DME info nav indicator. */
    dmeInfoIndicator: G3000DmeInfoNavIndicator;
    /** The number of supported DME radios. */
    dmeRadioCount: 0 | 1 | 2;
    /** A manager for DME user settings. */
    dmeSettingManager: UserSettingManager<DmeUserSettingTypes>;
    /** A manager for display units user settings. */
    unitsSettingManager: UnitsUserSettingManager;
    /** Whether the display should be decluttered. */
    declutter: Subscribable<boolean>;
}
/**
 * A G3000 NAV/DME information display.
 */
export declare class NavDmeInfo extends DisplayComponent<NavDmeInfoProps> {
    private readonly distanceRef;
    private readonly rootStyle;
    private readonly distanceStyle;
    private readonly rootCssClass;
    private readonly sourceFormatter;
    private readonly navSourceText;
    private readonly navIdentText;
    private readonly navFreqText;
    private readonly dmeSourceText;
    private readonly dmeDistance;
    private readonly dmeDistancePrecision;
    private readonly activeDmeTuneMode;
    private readonly isDmeTunedToNavSource;
    private readonly dmeFreqText;
    private declutterSub?;
    private navPreviewSub?;
    private dmePreviewSub?;
    private dmeDistanceSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=NavDmeInfo.d.ts.map