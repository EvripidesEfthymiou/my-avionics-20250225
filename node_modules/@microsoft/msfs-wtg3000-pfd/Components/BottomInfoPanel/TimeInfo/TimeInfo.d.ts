import { ComponentProps, DisplayComponent, Subscribable, UserSettingManager, VNode } from '@microsoft/msfs-sdk';
import { DateTimeUserSettingTypes } from '@microsoft/msfs-garminsdk';
import { TimeInfoDataProvider } from './TimeInfoDataProvider';
import './TimeInfo.css';
/**
 * Component props for TimeInfo.
 */
export interface TimeInfoProps extends ComponentProps {
    /** A data provider for the display. */
    dataProvider: TimeInfoDataProvider;
    /** A manager for date/time user settings. */
    dateTimeSettingManager: UserSettingManager<DateTimeUserSettingTypes>;
    /** Whether the display should be decluttered. */
    declutter: Subscribable<boolean>;
}
/**
 * A G3000 time information display. Displays the value of the generic timer and the current time.
 */
export declare class TimeInfo extends DisplayComponent<TimeInfoProps> {
    private static readonly MAX_TIMER_VALUE_SECONDS;
    private readonly timerRef;
    private readonly simTimeRef;
    private readonly rootCssClass;
    private readonly simTimeCssClass;
    private readonly timerValue;
    private readonly simTime;
    private readonly timeFormat;
    private readonly timeFormatText;
    private timerValuePipe?;
    private simTimePipe?;
    private formatSub?;
    private declutterSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /** @inheritdoc */
    render(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
//# sourceMappingURL=TimeInfo.d.ts.map