import { ComponentProps, DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import { NavIndicatorController } from '@microsoft/msfs-garminsdk';
import './DMEWindow.css';
/**
 * The properties for the DMEWindow
 * component.
 */
interface DMEWindowProps extends ComponentProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** An instance of the NavIndicatorController. */
    navIndicatorController: NavIndicatorController;
}
/**
 * The DMEWindow
 * component.
 */
export declare class DMEWindow extends DisplayComponent<DMEWindowProps> {
    private navSource;
    private navFreq;
    private navDist;
    private dmeElement;
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    /**
     * Inits the DME Display.
     */
    private init;
    /**
     * Updated the DME Display.
     * @param toggle If the DME button has been turned on or off
     */
    private updateDMEDisplay;
    /**
     * Offsets the display to the left when the HSI map is active to prevent it from being obscured
     * @param isHSIMAP Boolean if HSI map is active or not
     */
    private setOffset;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=DMEWindow.d.ts.map