import { AirportFacility, ComponentProps, DisplayComponent, EventBus, FacilityLoader, GeoPointSubject, NodeReference, VNode } from '@microsoft/msfs-sdk';
import { PlaneStateInfo } from '../../FlightInstruments/PrimaryHorizonDisplay';
import { SvtAirportLabel } from './SvtAirportLabel';
import './SvtAirportLabels.css';
/**
 * The properties for the SvtAirportLabels component.
 */
interface SvtAirportLabelsProps extends ComponentProps {
    /** The event bus. */
    bus: EventBus;
}
/**
 * The SvtAirportLabels component.
 */
export declare class SvtAirportLabels extends DisplayComponent<SvtAirportLabelsProps> {
    readonly planePos: GeoPointSubject;
    private pxPerDegY;
    loader: FacilityLoader;
    private session?;
    nearestFacilities: Map<string, AirportFacility>;
    facilityElements: Map<string, NodeReference<SvtAirportLabel>>;
    private static searchRange;
    private static maxFound;
    private containerRef;
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    /**
     * Updates the airport labels.
     * @param planeState The plane state info.
     */
    update(planeState: PlaneStateInfo): void;
    /**
     * Update our nearest airport list.
     */
    searchNearest(): void;
    /**
     * Update our nearest list with the latest search results.
     * @param results The results from a nearest search.
     * @returns A promise that resolves when the nearest list is updated.
     */
    private updateNearestFacilities;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=SvtAirportLabels.d.ts.map