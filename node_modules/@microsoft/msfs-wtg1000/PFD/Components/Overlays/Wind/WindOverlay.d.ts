import { DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import './WindOverlay.css';
/** The properties on the wind overlay component */
interface WindOverlayProps {
    /** An instance of the event bus. */
    bus: EventBus;
}
/** The PFD wind overlay. */
export declare class WindOverlay extends DisplayComponent<WindOverlayProps> {
    private option1Ref;
    private option2Ref;
    private option3Ref;
    private windBoxRef;
    private noDataRef;
    private readonly store;
    private readonly controller;
    /**
     * Creates an instance of Wind Overlay.
     * @param props The props.
     */
    constructor(props: WindOverlayProps);
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=WindOverlay.d.ts.map