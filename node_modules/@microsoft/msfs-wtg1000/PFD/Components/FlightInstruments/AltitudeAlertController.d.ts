import { EventBus, Subject } from '@microsoft/msfs-sdk';
/** The state of the altitude alerter. */
export declare enum AltAlertState {
    /** Disabled Mode */
    DISABLED = 0,
    /** Armed Mode. */
    ARMED = 1,
    /** Within 1000 feet of preselected altitude. */
    WITHIN_1000 = 2,
    /** Within 200 feet of preselected altitude. */
    WITHIN_200 = 3,
    /** Captured the preselected altitude. */
    CAPTURED = 4,
    /**Outside of the 200ft deviation zone */
    DEVIATION_200 = 5
}
/** Class to manage the altitude alerter on the PFD Altimeter */
export declare class AltitudeAlertController {
    private bus;
    isOnGround: boolean;
    alerterState: Subject<AltAlertState>;
    approachActive: boolean;
    altitudeLocked: boolean;
    altitude: number;
    selectedAltitude: number;
    targetAltitude: number;
    /**
     * Instantiates an instance of the AltitudeAlertController
     * @param bus is the event bus
     */
    constructor(bus: EventBus);
    /**
     * A method called to update the altitude alerter
     */
    updateAltitudeAlerter(): void;
}
//# sourceMappingURL=AltitudeAlertController.d.ts.map