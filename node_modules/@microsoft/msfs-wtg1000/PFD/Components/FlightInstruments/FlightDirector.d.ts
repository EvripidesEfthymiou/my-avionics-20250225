import { DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import './FlightDirector.css';
/**
 * The properties for the flight director component.
 */
interface FlightDirectorProps {
    /** An instance of the event bus. */
    bus: EventBus;
}
/**
 * The PFD Flight Director.
 */
export declare class FlightDirector extends DisplayComponent<FlightDirectorProps> {
    private fltDirectorRef;
    private currentBank;
    private fdRawBank;
    private fdServoBank;
    private currentPitch;
    private pitchIncrementDistance;
    private fdRawPitch;
    private bankServo;
    private pitchAverage;
    private needUpdate;
    private readonly fdPitchHandler;
    private readonly fdBankHandler;
    private readonly pitchHandler;
    private readonly bankHandler;
    private readonly subscriptions;
    /** @inheritdoc */
    constructor(props: FlightDirectorProps);
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    /**
     * A callback called when the Flight Director is turned on or off.
     * @param state The bool state of the flight director status.
     */
    private toggleFltDir;
    /**
     * Updates the flight director.
     */
    private updateFD;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=FlightDirector.d.ts.map