import { DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import './MarkerBeacon.css';
/**
 * The properties for the marker beacon component.
 */
interface MarkerBeaconProps {
    /** An instance of the event bus. */
    bus: EventBus;
}
/**
 * The PFD vertical deviation indicator.
 */
export declare class MarkerBeacon extends DisplayComponent<MarkerBeaconProps> {
    private static readonly TEXT;
    private readonly mkrBcnBoxRef;
    private readonly textSub;
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    /**
     * A method called when the marker beacon state has changed.
     * @param state is the marker beacon state.
     */
    private onMarkerBeacon;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=MarkerBeacon.d.ts.map