import { EventBus, Subject, VNode } from '@microsoft/msfs-sdk';
import { NavIndicatorController } from '@microsoft/msfs-garminsdk';
import { UiView, UiViewProps } from '../../../../Shared/UI/UiView';
import './ADFDME.css';
/**
 * The properties for the ADFDME
 * component.
 */
interface ADFDMEProps extends UiViewProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** An instance of the NavIndicatorController. */
    navIndicatorController: NavIndicatorController;
}
/**
 * The ADFDME
 * component.
 */
export declare class ADFDME extends UiView<ADFDMEProps> {
    private adfdmeToggleOptions;
    private dmeToggleOptions;
    private freqComponentRef;
    adfInputSubject: Subject<number>;
    private adfActiveFreq;
    enterToTransferSubject: Subject<string>;
    /**
     * A callback called after the component renders.
     */
    onAfterRender(): void;
    private onADFToggleSelected;
    private onDmeToggleSelected;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=ADFDME.d.ts.map