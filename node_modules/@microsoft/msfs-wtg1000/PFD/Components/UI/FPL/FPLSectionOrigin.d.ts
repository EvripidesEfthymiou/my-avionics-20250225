import { ComponentProps, DisplayComponent, FlightPlan, FlightPlanOriginDestEvent, LegDefinition, VNode } from '@microsoft/msfs-sdk';
/**
 * Represent the origin segment of a flight plan listing.
 */
export declare class FPLOrigin extends DisplayComponent<ComponentProps> {
    private origin;
    private destination;
    private holdDistance;
    private holdTime;
    private holdCourse;
    private fplDirectTo;
    private origDestRef;
    private fplDirectHold;
    private holdSvg;
    /**
     * Handles a origin/dest change event.
     * @param e The change event itself.
     */
    onOriginDestChanged(e: FlightPlanOriginDestEvent): void;
    /**
     * Handles when a random direct to is active and the FPL header should indicate such.
     * @param dtoIdent The ident for the direct to waypoint.
     * @param lastLeg The last leg in the direct to random segment.
     */
    onDirectToRandomActive(dtoIdent: string, lastLeg: LegDefinition | null): void;
    /**
     * Handles when a random direct to is canceled.
     * @param plan is the Flightplan
     */
    removeDirectToRandom(plan: FlightPlan): void;
    /**
     * Handles setting the origin string.
     * @param origin The ident for the origin.
     */
    setOrigin(origin: string): void;
    /**
     * Handles setting the destination string.
     * @param destination The ident for the origin.
     */
    setDestination(destination: string): void;
    /**
     * Initializes the Direct To symbol to hidden
     */
    onAfterRender(): void;
    /**
     * Render an origin block.
     * @returns a VNode.
     */
    render(): VNode;
}
//# sourceMappingURL=FPLSectionOrigin.d.ts.map