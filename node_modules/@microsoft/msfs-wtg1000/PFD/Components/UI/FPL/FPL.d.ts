import { EventBus, VNode } from '@microsoft/msfs-sdk';
import { Fms } from '@microsoft/msfs-garminsdk';
import { FmsHEvent } from '../../../../Shared/UI/FmsHEvent';
import { UiView, UiViewProps } from '../../../../Shared/UI/UiView';
import './FPL.css';
/** The properties on the flight plan popout component. */
interface FPLPopupProps extends UiViewProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** An FMS state manager. */
    fms: Fms;
}
/**
 * The FPL popup container encapsulates the actual popup logic.
 */
export declare class FPL extends UiView<FPLPopupProps> {
    private readonly fplDetailsRef;
    /** @inheritdoc */
    onInteractionEvent(evt: FmsHEvent): boolean;
    /** Called when the view is resumed. */
    onViewResumed(): void;
    /** Called when the view is closed. */
    onViewClosed(): void;
    /** Called when the view is opened. */
    onViewOpened(): void;
    /**
     * Renders the component.
     * @returns The component VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=FPL.d.ts.map