import { EventBus, VNode } from '@microsoft/msfs-sdk';
import { UiViewProps } from '../../../../Shared/UI/UiView';
import { WptDupDialog } from '../../../../Shared/UI/WptDup/WptDupDialog';
import { UnitsUserSettingManager } from '../../../../Shared/Units/UnitsUserSettings';
import './MFDWptDupDialog.css';
/**
 * Component props for MFDWaypointDuplicateDialog.
 */
export interface MFDWptDupDialogProps extends UiViewProps {
    /** The event bus. */
    bus: EventBus;
}
/**
 * A dialog for selecting from a list of duplicate waypoints.
 */
export declare class MFDWptDupDialog extends WptDupDialog<MFDWptDupDialogProps> {
    private static readonly tempGeoPoint;
    private readonly selectedWaypointSub;
    private readonly planePos;
    protected readonly unitSettingManager: UnitsUserSettingManager<import("@microsoft/msfs-garminsdk").UnitsUserSettingTypes>;
    private readonly planePosSub;
    /** @inheritdoc */
    protected onViewOpened(): void;
    /** @inheritdoc */
    protected onViewClosed(): void;
    /**
     * A callback which is called when the plane's current position changes.
     * @param pos The new position.
     */
    private onPlanePosChanged;
    /**
     * A callback which is called when the selected waypoint changes.
     * @param waypoint The new selected waypoint.
     */
    private onWaypointSelected;
    /** @inheritdoc */
    render(): VNode;
}
//# sourceMappingURL=MFDWptDupDialog.d.ts.map