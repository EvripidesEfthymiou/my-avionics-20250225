import { EventBus, VNode } from '@microsoft/msfs-sdk';
import { MFDUiPage, MFDUiPageProps } from './MFDUiPage';
/**
 * Props on the TurbulenceGraph component.
 */
interface TurbulenceGraphProps extends MFDUiPageProps {
    /** An instance of the EventBus. */
    bus: EventBus;
}
/**
 * A graph of turbulence.
 */
export declare class TurbulenceGraph extends MFDUiPage<TurbulenceGraphProps> {
    private readonly canvas;
    private readonly samples;
    private average;
    private max;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /**
     * Draws the graph.
     */
    private draw;
    /**
     * Draws a scale line.
     * @param context The context.
     * @param accel The acceleration.
     */
    private drawScaleLine;
    /** @inheritdoc */
    render(): VNode;
}
export {};
//# sourceMappingURL=TurbulenceGraph.d.ts.map