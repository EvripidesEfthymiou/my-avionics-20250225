import { LegDefinition, Subscribable, VNode } from '@microsoft/msfs-sdk';
import { UiControl, UiControlProps } from '../../../../Shared/UI/UiControl';
import { UnitsUserSettingManager } from '../../../../Shared/Units/UnitsUserSettings';
import './ProcSequenceItem.css';
/**
 * The properties for the ProcSequenceItem component.
 */
interface ProcSequenceItemProps extends UiControlProps {
    /**
     * The actual data object for this fix
     * @type {LegDefinition}
     */
    data: Subscribable<LegDefinition>;
    /** A display units user setting manager. */
    unitsSettingManager: UnitsUserSettingManager;
}
/** The Procedure Sequence component. */
export declare class ProcSequenceItem extends UiControl<ProcSequenceItemProps> {
    private static readonly DISTANCE_FORMATTER;
    private static readonly BEARING_FORMATTER;
    private static readonly nmCache;
    private static readonly magBearingCache;
    private readonly nameRef;
    private readonly fixTypeSub;
    private readonly dtkSub;
    private readonly distanceSub;
    private readonly visibilityHandler;
    private readonly distanceVisibilityHandler;
    private readonly dtkVisibilityHandler;
    private readonly dataSub;
    /**
     * Creates an instance of FixInfo.
     * @param props The props of the component.
     */
    constructor(props: ProcSequenceItemProps);
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Gets the container element location
     * @returns An array of x,y.
     */
    getContainerElementLocation(): [number, number];
    /** @inheritdoc */
    getHighlightElement(): Element | null;
    /** @inheritdoc */
    protected renderControl(): VNode;
    /** @inheritdoc */
    destroy(): void;
}
export {};
//# sourceMappingURL=ProcSequenceItem.d.ts.map