import { ComponentProps, DisplayComponent, IntersectionFacility, VNode } from '@microsoft/msfs-sdk';
import { UnitsUserSettingManager } from '../../../../../Shared/Units/UnitsUserSettings';
import './ReferenceVorGroup.css';
/** Props on the ReferenceVorGroup component. */
export interface NearestIntersectionReferenceVorGroupProps extends ComponentProps {
    /** A user setting manager for measurement units. */
    unitsSettingManager: UnitsUserSettingManager;
}
/**
 * A component that displays the reference VOR information for an intersection
 * on the MFD nearest intersections page.
 */
export declare class NearestIntersectionReferenceVorGroup extends DisplayComponent<NearestIntersectionReferenceVorGroupProps> {
    private static readonly VOR_ICON_SRC_MAP;
    private readonly content;
    private readonly name;
    private readonly iconSrc;
    private readonly frequency;
    private readonly bearing;
    private readonly distance;
    private facilityPos;
    /**
     * Sets the facility to display in this group.
     * @param facility The facility to display.
     */
    set(facility: IntersectionFacility | null): void;
    /** @inheritdoc */
    render(): VNode;
}
//# sourceMappingURL=ReferenceVorGroup.d.ts.map