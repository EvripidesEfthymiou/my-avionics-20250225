import { AirportFacility, ApproachProcedure, VNode } from '@microsoft/msfs-sdk';
import { G1000UiControl, G1000UiControlProps } from '../../../../../Shared/UI/G1000UiControl';
import './ApproachesGroup.css';
/**
 * Props on the ApproachesGroup component.
 */
export interface NearestAirportApproachesGroupProps extends G1000UiControlProps {
    /** A callback called when an approach is selected. */
    onSelected: (approach: ApproachProcedure | undefined) => void;
}
/**
 * A component that displays the available approaches on a nearest airport on
 * the MFD nearest airports page.
 */
export declare class NearestAirportApproachesGroup extends G1000UiControl<NearestAirportApproachesGroupProps> {
    private readonly approaches;
    private readonly approachList;
    private facility;
    /** @inheritdoc */
    onEnter(): boolean;
    /**
     * Sets the airport facility to display in this group.
     * @param facility The facility to display.
     */
    set(facility: AirportFacility | null): void;
    /**
     * Builds an approach item in the nearest airports approaches list.
     * @param approach The approach to build the list item for.
     * @returns A new approach list item.
     * @throws An error if the facility is undefined.
     */
    private buildApproachItem;
    /** @inheritdoc */
    render(): VNode;
}
//# sourceMappingURL=ApproachesGroup.d.ts.map