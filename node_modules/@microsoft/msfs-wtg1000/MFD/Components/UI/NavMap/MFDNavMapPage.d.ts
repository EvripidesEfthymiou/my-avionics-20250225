import { EventBus, FlightPlanner, VNode } from '@microsoft/msfs-sdk';
import { TrafficAdvisorySystem } from '@microsoft/msfs-garminsdk';
import { FmsHEvent } from '../../../../Shared/UI/FmsHEvent';
import { MFDUiPage, MFDUiPageProps } from '../MFDUiPage';
import './MFDNavMapPage.css';
/**
 * Component props for MFDNavMapPage.
 */
export interface MFDNavMapPageProps extends MFDUiPageProps {
    /** The event bus. */
    bus: EventBus;
    /** An instance of the flight planner. */
    flightPlanner: FlightPlanner;
    /** The G1000 traffic advisory system. */
    tas: TrafficAdvisorySystem;
}
/**
 * A page which displays the navigation map.
 */
export declare class MFDNavMapPage extends MFDUiPage<MFDNavMapPageProps> {
    private static readonly DECLUTTER_TEXT;
    private static readonly UPDATE_FREQ;
    private static readonly POINTER_MOVE_INCREMENT;
    private readonly mapSettingManager;
    private readonly compiledMap;
    private readonly mapRangeSetting;
    private readonly mapRangeModule;
    private readonly mapPointerModule;
    private readonly mapPointerController;
    private readonly pageMenuItems;
    /** @inheritdoc */
    constructor(props: MFDNavMapPageProps);
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    onInteractionEvent(evt: FmsHEvent): boolean;
    /**
     * Changes the MFD map range index setting.
     * @param delta The change in index to apply.
     */
    private changeMapRangeIndex;
    /**
     * Handles events that move the map pointer.
     * @param evt The event.
     * @returns Whether the event was handled.
     */
    private handleMapPointerMoveEvent;
    /** @inheritdoc */
    protected onViewOpened(): void;
    /** @inheritdoc */
    protected onViewClosed(): void;
    /** @inheritdoc */
    protected onMenuPressed(): boolean;
    /** @inheritdoc */
    render(): VNode;
}
//# sourceMappingURL=MFDNavMapPage.d.ts.map