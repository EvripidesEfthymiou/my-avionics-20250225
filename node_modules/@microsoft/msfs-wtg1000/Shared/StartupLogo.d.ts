import { DisplayComponent, EventBus, VNode } from '@microsoft/msfs-sdk';
import './StartupLogo.css';
/**
 * Props on the StartupLogo component.
 */
interface StartupLogoProps {
    /** An instance of the event bus. */
    bus: EventBus;
    /** An event fired when the screen is confirmed. */
    onConfirmation?: () => void;
    /** The prefix for H events to process. */
    eventPrefix: 'AS1000_PFD' | 'AS1000_MFD';
}
/**
 * A component that displays the Garmin startup logo.
 */
export declare class StartupLogo extends DisplayComponent<StartupLogoProps> {
    private readonly el;
    private readonly mainLogo;
    private readonly mfdScreen;
    private screenState;
    private hEventSub?;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Handles when an H event arrives to check for startup screen confirmation.
     * @param evt The event that arrived.
     */
    private handleHEvent;
    /** @inheritdoc */
    render(): VNode;
}
export {};
//# sourceMappingURL=StartupLogo.d.ts.map