import { APStateManager, EventBus, FlightPlanner, Subject } from '@microsoft/msfs-sdk';
import { GarminAPConfigInterface, GarminAutopilot, GarminAutopilotOptions } from '@microsoft/msfs-garminsdk';
/**
 * A Garmin GFC700 autopilot.
 */
export declare class G1000Autopilot extends GarminAutopilot {
    readonly externalAutopilotInstalled: Subject<boolean>;
    private fmaUpdateDebounce;
    /**
     * Creates an instance of the G1000Autopilot.
     * @param bus The event bus.
     * @param flightPlanner This autopilot's associated flight planner.
     * @param config This autopilot's configuration.
     * @param stateManager This autopilot's state manager.
     * @param options Options with which to configure the new autopilot.
     */
    constructor(bus: EventBus, flightPlanner: FlightPlanner, config: GarminAPConfigInterface, stateManager: APStateManager, options?: Readonly<GarminAutopilotOptions>);
    /** @inheritdoc */
    protected onAfterUpdate(): void;
    /** @inheritdoc */
    protected monitorAdditionalEvents(): void;
    /**
     * Called when an external AP installed event is received from the bus.
     * @param installed Whether an external AP is installed.
     */
    private setExternalAutopilotInstalled;
    /**
     * Publishes data for the FMA.
     * @param clear Is to clear the FMA
     */
    private updateG1000Fma;
}
//# sourceMappingURL=G1000Autopilot.d.ts.map