import { EventBus } from '@microsoft/msfs-sdk';
/**
 * A controller which handles automatic functions of the weather radar.
 */
export declare class WeatherRadarController {
    private readonly bus;
    private readonly settingManager;
    private readonly isActiveSetting;
    private readonly operatingModeSetting;
    private readonly scanModeSetting;
    private readonly isOnGround;
    private isAutoStandbyArmed;
    /**
     * Constructor.
     * @param bus The event bus.
     */
    constructor(bus: EventBus);
    /**
     * Destroys this controller. Once destroyed, this controller will no longer control the weather radar.
     */
    destroy(): void;
}
//# sourceMappingURL=WeatherRadarController.d.ts.map