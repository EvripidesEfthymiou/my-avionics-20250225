import { EventBus, PublishPacer, SimVarPublisher } from '@microsoft/msfs-sdk';
/** Data related to autopilot */
export interface G1000APSimVarEvents {
    /** Whether the WT KAP140 autopilot is installed. */
    kap_140_installed: boolean;
    /** Whether the WT KAP140 autopilot is installed (old simvar). */
    kap_140_installed_old: boolean;
}
/**
 * A publisher for AP information for the Nxi.
 */
export declare class G1000APPublisher extends SimVarPublisher<G1000APSimVarEvents> {
    private static readonly SIMVARS;
    /**
     * Creates an AhrsPublisher.
     * @param bus The event bus to which to publish.
     * @param pacer An optional pacer to use to control the rate of publishing.
     */
    constructor(bus: EventBus, pacer?: PublishPacer<G1000APSimVarEvents>);
}
//# sourceMappingURL=G1000APPublisher.d.ts.map