import { FacilityWaypoint, GeoPointInterface, Subject, Subscribable, Waypoint } from '@microsoft/msfs-sdk';
import { WaypointInfoStore } from '@microsoft/msfs-garminsdk';
/** The store for the DTO view */
export declare class DirectToStore {
    readonly planePos: Subscribable<GeoPointInterface>;
    readonly waypointInfoStore: WaypointInfoStore;
    readonly courseInputValue: Subject<number>;
    readonly course: Subject<number | undefined>;
    /** A subject which provides this store's selected waypoint. */
    get waypoint(): Subject<Waypoint | null>;
    private _matchedWaypoints;
    /** An array of waypoints which have matched the input. */
    get matchedWaypoints(): readonly FacilityWaypoint[];
    /**
     * Constructor.
     * @param planePos A subscribable which provides the current airplane position for this store.
     */
    constructor(planePos: Subscribable<GeoPointInterface>);
    /**
     * Set the list of matched waypoints.
     * @param waypoints An array of matched waypoints.
     */
    setMatchedWaypoints(waypoints: readonly FacilityWaypoint[]): void;
}
//# sourceMappingURL=DirectToStore.d.ts.map