import { EventBus } from '@microsoft/msfs-sdk';
/**
 * Controller that maps control pad input to the xpdr bus events.
 */
export declare class XpdrInputController {
    private readonly bus;
    private readonly isPfd;
    private readonly controlPadXpdrInputMap;
    private readonly validXpdrDigits;
    private digitPosition;
    private readonly g1000ControlPublisher;
    /**
     * Constructs the controller.
     * @param bus The event bus.
     * @param isPfd true if the controller is for the PFD, false if for the MFD
     */
    constructor(bus: EventBus, isPfd: boolean);
    /**
     * Enters XPDR Entry mode
     */
    startXpdrEntry(): void;
    /**
     * Handles the XPDR input event
     * @param hEvent received hEvent
     * @returns true if handled, false if the not
     */
    handleXpdrEntry(hEvent: string): boolean;
    /**
     * Publishes the event to the bus if the controller is for the PFD.
     * @param event the event to publish
     * @param value the value to publish
     */
    private publishEvent;
}
//# sourceMappingURL=XpdrInputController.d.ts.map