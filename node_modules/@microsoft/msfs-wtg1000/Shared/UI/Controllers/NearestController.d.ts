import { AirportFacility, ControlPublisher } from '@microsoft/msfs-sdk';
import { ViewService } from '../ViewService';
import { NearestStore } from './NearestStore';
/** A nearest controleer */
export declare class NearestController {
    private readonly viewService;
    private store;
    private publisher;
    readonly onDirectIdentHandler: (airport: AirportFacility | null) => boolean;
    readonly onEnterFreqHandler: (value: string) => boolean;
    /**
     * Creates one.
     * @param store the store
     * @param publisher A ControlPublisher for freq set events.
     * @param viewService The View Service.
     */
    constructor(store: NearestStore, publisher: ControlPublisher, viewService: ViewService);
    /**
     * A callback which is called when a DRCT input is made on a nearest airport.
     * @param airport The airport.
     * @returns Whether the event was handled.
     */
    private onDirectIdent;
    /**
     * A callback which is called when an ENTER input is made on a nearest airport frequency.
     * @param value The frequency.
     * @returns Whether the event was handled.
     */
    private onEnterFreq;
}
//# sourceMappingURL=NearestController.d.ts.map