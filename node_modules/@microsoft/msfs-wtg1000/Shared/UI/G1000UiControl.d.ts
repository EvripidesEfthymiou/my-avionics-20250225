import { HardwareControlListProps, HardwareUiControl, HardwareUiControlList, HardwareUiControlProps, UiControlEventHandler, UiControlEventHandlers, UiControlPropEventHandlers, VNode } from '@microsoft/msfs-sdk';
import { FmsHEvent } from './FmsHEvent';
import './G1000UiControlList.css';
/**
 * HardwareUiControl events for Fms interaction H events.
 */
export type FmsUiControlEvents = Record<FmsHEvent, UiControlEventHandler<G1000UiControl>>;
/** Properties on the G1000UiControl component. */
export interface G1000UiControlProps extends UiControlPropEventHandlers<FmsUiControlEvents>, HardwareUiControlProps {
}
/**
 * A component that forms the base of the G1000 UI control system.
 */
export declare class G1000UiControl<P extends G1000UiControlProps = G1000UiControlProps> extends HardwareUiControl<FmsUiControlEvents, P> implements UiControlEventHandlers<FmsUiControlEvents> {
    /** @inheritdoc */
    onInteractionEvent(evt: FmsHEvent): boolean;
    /**
     * Handles FMS upper knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles FMS upper knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles FMS lower knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onLowerKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles FMS lower knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onLowerKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles FMS upper knob push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobPush(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobPush(source: G1000UiControl): boolean;
    /**
     * Handles Outer Control Knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onOuterControlKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles Outer Control Knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onOuterControlKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles MENU button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onMenu(source: G1000UiControl): boolean;
    /**
     * Handles ENTER button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onEnter(source: G1000UiControl): boolean;
    /**
     * Handles CLR button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onClr(source: G1000UiControl): boolean;
    /**
     * Handles CLR button long press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onClrLong(source: G1000UiControl): boolean;
    /**
     * Handles DRCT button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onDirectTo(source: G1000UiControl): boolean;
    /**
     * Handles FPL button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onFPL(source: G1000UiControl): boolean;
    /**
     * Handles PROC button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onProc(source: G1000UiControl): boolean;
    /**
     * Handles range joystick increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onRangeInc(source: G1000UiControl): boolean;
    /**
     * Handles range joystick decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onRangeDec(source: G1000UiControl): boolean;
    /**
     * Handles range joystick push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickPush(source: G1000UiControl): boolean;
    /**
     * Handles range joystick left deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickLeft(source: G1000UiControl): boolean;
    /**
     * Handles range joystick up deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickUp(source: G1000UiControl): boolean;
    /**
     * Handles range joystick right deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickRight(source: G1000UiControl): boolean;
    /**
     * Handles range joystick down deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickDown(source: G1000UiControl): boolean;
    /**
     * Handles the A key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onA(source: G1000UiControl): boolean;
    /**
     * Handles the B key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onB(source: G1000UiControl): boolean;
    /**
     * Handles the C key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onC(source: G1000UiControl): boolean;
    /**
     * Handles the D key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onD(source: G1000UiControl): boolean;
    /**
     * Handles the E key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onE(source: G1000UiControl): boolean;
    /**
     * Handles the F key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onF(source: G1000UiControl): boolean;
    /**
     * Handles the G key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onG(source: G1000UiControl): boolean;
    /**
     * Handles the H key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onH(source: G1000UiControl): boolean;
    /**
     * Handles the I key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onI(source: G1000UiControl): boolean;
    /**
     * Handles the J key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJ(source: G1000UiControl): boolean;
    /**
     * Handles the K key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onK(source: G1000UiControl): boolean;
    /**
     * Handles the L key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onL(source: G1000UiControl): boolean;
    /**
     * Handles the M key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onM(source: G1000UiControl): boolean;
    /**
     * Handles the N key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onN(source: G1000UiControl): boolean;
    /**
     * Handles the O key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onO(source: G1000UiControl): boolean;
    /**
     * Handles the P key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onP(source: G1000UiControl): boolean;
    /**
     * Handles the Q key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onQ(source: G1000UiControl): boolean;
    /**
     * Handles the R key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onR(source: G1000UiControl): boolean;
    /**
     * Handles the S key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onS(source: G1000UiControl): boolean;
    /**
     * Handles the T key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onT(source: G1000UiControl): boolean;
    /**
     * Handles the U key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onU(source: G1000UiControl): boolean;
    /**
     * Handles the V key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onV(source: G1000UiControl): boolean;
    /**
     * Handles the W key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onW(source: G1000UiControl): boolean;
    /**
     * Handles the X key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onX(source: G1000UiControl): boolean;
    /**
     * Handles the Y key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onY(source: G1000UiControl): boolean;
    /**
     * Handles the Z key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onZ(source: G1000UiControl): boolean;
    /**
     * Handles the  key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onSPC(source: G1000UiControl): boolean;
    /**
     * Handles the 0 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on0(source: G1000UiControl): boolean;
    /**
     * Handles the 1 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on1(source: G1000UiControl): boolean;
    /**
     * Handles the 2 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on2(source: G1000UiControl): boolean;
    /**
     * Handles the 3 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on3(source: G1000UiControl): boolean;
    /**
     * Handles the 4 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on4(source: G1000UiControl): boolean;
    /**
     * Handles the 5 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on5(source: G1000UiControl): boolean;
    /**
     * Handles the 6 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on6(source: G1000UiControl): boolean;
    /**
     * Handles the 7 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on7(source: G1000UiControl): boolean;
    /**
     * Handles the 8 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on8(source: G1000UiControl): boolean;
    /**
     * Handles the 9 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on9(source: G1000UiControl): boolean;
    /**
     * Handles the Dot key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onDot(source: G1000UiControl): boolean;
    /**
     * Handles the BKSP key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onBKSP(source: G1000UiControl): boolean;
    /**
     * Handles the +/- key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onPlusMinus(source: G1000UiControl): boolean;
    /**
     * Handles the Home key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onHome(source: G1000UiControl): boolean;
    /**
     * Handles the COM key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onCom(source: G1000UiControl): boolean;
    /**
     * Handles the NAV key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onNav(source: G1000UiControl): boolean;
    /**
     * Handles the XPDR key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onXpdr(source: G1000UiControl): boolean;
    /**
     * Handles the CRS key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onCrs(source: G1000UiControl): boolean;
    /**
     * Consolidates all key events and allows sub classes to override and implement specific keyboard input behaviors.
     * @param source The source of the event.
     * @param evt FmsHEvent of the keyboard event.
     * @returns always false for the top level version of the method.
     */
    consolidateKeyboardHEvent(source: G1000UiControl, evt: FmsHEvent): boolean;
}
/** Properties on the GarminControlList component. */
export interface GarminControlListProps<T> extends UiControlPropEventHandlers<FmsUiControlEvents>, HardwareUiControlProps, HardwareControlListProps<T> {
}
/**
 * A component that holds lists of G1000UiControls.
 */
export declare class G1000ControlList<T> extends HardwareUiControlList<T, FmsUiControlEvents, GarminControlListProps<T>> implements UiControlEventHandlers<FmsUiControlEvents> {
    /** @inheritdoc */
    onInteractionEvent(evt: FmsHEvent): boolean;
    /**
     * Handles FMS upper knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles FMS upper knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles FMS lower knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onLowerKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles FMS lower knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onLowerKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles FMS upper knob push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onUpperKnobPush(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles Inner Control Knob push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onInnerControlKnobPush(source: G1000UiControl): boolean;
    /**
     * Handles Outer Control Knob decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onOuterControlKnobDec(source: G1000UiControl): boolean;
    /**
     * Handles Outer Control Knob increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onOuterControlKnobInc(source: G1000UiControl): boolean;
    /**
     * Handles MENU button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onMenu(source: G1000UiControl): boolean;
    /**
     * Handles ENTER button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onEnter(source: G1000UiControl): boolean;
    /**
     * Handles CLR button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onClr(source: G1000UiControl): boolean;
    /**
     * Handles CLR button long press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onClrLong(source: G1000UiControl): boolean;
    /**
     * Handles DRCT button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onDirectTo(source: G1000UiControl): boolean;
    /**
     * Handles FPL button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onFPL(source: G1000UiControl): boolean;
    /**
     * Handles PROC button press events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onProc(source: G1000UiControl): boolean;
    /**
     * Handles range joystick increase events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onRangeInc(source: G1000UiControl): boolean;
    /**
     * Handles range joystick decrease events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onRangeDec(source: G1000UiControl): boolean;
    /**
     * Handles range joystick push events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickPush(source: G1000UiControl): boolean;
    /**
     * Handles range joystick left deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickLeft(source: G1000UiControl): boolean;
    /**
     * Handles range joystick up deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickUp(source: G1000UiControl): boolean;
    /**
     * Handles range joystick right deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickRight(source: G1000UiControl): boolean;
    /**
     * Handles range joystick down deflection events.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJoystickDown(source: G1000UiControl): boolean;
    /**
     * Handles the A key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onA(source: G1000UiControl): boolean;
    /**
     * Handles the B key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onB(source: G1000UiControl): boolean;
    /**
     * Handles the C key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onC(source: G1000UiControl): boolean;
    /**
     * Handles the D key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onD(source: G1000UiControl): boolean;
    /**
     * Handles the E key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onE(source: G1000UiControl): boolean;
    /**
     * Handles the F key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onF(source: G1000UiControl): boolean;
    /**
     * Handles the G key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onG(source: G1000UiControl): boolean;
    /**
     * Handles the H key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onH(source: G1000UiControl): boolean;
    /**
     * Handles the I key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onI(source: G1000UiControl): boolean;
    /**
     * Handles the J key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onJ(source: G1000UiControl): boolean;
    /**
     * Handles the K key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onK(source: G1000UiControl): boolean;
    /**
     * Handles the L key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onL(source: G1000UiControl): boolean;
    /**
     * Handles the M key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onM(source: G1000UiControl): boolean;
    /**
     * Handles the N key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onN(source: G1000UiControl): boolean;
    /**
     * Handles the O key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onO(source: G1000UiControl): boolean;
    /**
     * Handles the P key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onP(source: G1000UiControl): boolean;
    /**
     * Handles the Q key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onQ(source: G1000UiControl): boolean;
    /**
     * Handles the R key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onR(source: G1000UiControl): boolean;
    /**
     * Handles the S key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onS(source: G1000UiControl): boolean;
    /**
     * Handles the T key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onT(source: G1000UiControl): boolean;
    /**
     * Handles the U key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onU(source: G1000UiControl): boolean;
    /**
     * Handles the V key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onV(source: G1000UiControl): boolean;
    /**
     * Handles the W key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onW(source: G1000UiControl): boolean;
    /**
     * Handles the X key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onX(source: G1000UiControl): boolean;
    /**
     * Handles the Y key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onY(source: G1000UiControl): boolean;
    /**
     * Handles the Z key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onZ(source: G1000UiControl): boolean;
    /**
     * Handles the  key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onSPC(source: G1000UiControl): boolean;
    /**
     * Handles the 0 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on0(source: G1000UiControl): boolean;
    /**
     * Handles the 1 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on1(source: G1000UiControl): boolean;
    /**
     * Handles the 2 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on2(source: G1000UiControl): boolean;
    /**
     * Handles the 3 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on3(source: G1000UiControl): boolean;
    /**
     * Handles the 4 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on4(source: G1000UiControl): boolean;
    /**
     * Handles the 5 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on5(source: G1000UiControl): boolean;
    /**
     * Handles the 6 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on6(source: G1000UiControl): boolean;
    /**
     * Handles the 7 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on7(source: G1000UiControl): boolean;
    /**
     * Handles the 8 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on8(source: G1000UiControl): boolean;
    /**
     * Handles the 9 key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    on9(source: G1000UiControl): boolean;
    /**
     * Handles the Dot key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onDot(source: G1000UiControl): boolean;
    /**
     * Handles the BKSP key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onBKSP(source: G1000UiControl): boolean;
    /**
     * Handles the +/- key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onPlusMinus(source: G1000UiControl): boolean;
    /**
     * Handles the Home key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onHome(source: G1000UiControl): boolean;
    /**
     * Handles the COM key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onCom(source: G1000UiControl): boolean;
    /**
     * Handles the NAV key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onNav(source: G1000UiControl): boolean;
    /**
     * Handles the XPDR key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onXpdr(source: G1000UiControl): boolean;
    /**
     * Handles the CRS key.
     * @param source The source of the event.
     * @returns Whether the event was handled.
     */
    onCrs(source: G1000UiControl): boolean;
    /**
     * Consolidates all key events and allows sub classes to override and implement specific keyboard input behaviors.
     * @param source The source of the event.
     * @param evt FmsHEvent of the keyboard event.
     * @returns always false for the top level version of the method.
     */
    consolidateKeyboardHEvent(source: G1000UiControl, evt: FmsHEvent): boolean;
    /** @inheritdoc */
    protected renderScrollbar(): VNode;
}
//# sourceMappingURL=G1000UiControl.d.ts.map