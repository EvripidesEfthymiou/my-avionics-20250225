import { ArraySubject, EventBus, FacilityLoader, FlightPlanSegment, Subject } from '@microsoft/msfs-sdk';
import { ActiveLegDefinition, ActiveLegStates } from '../UIControls/FplActiveLegArrow';
import { FacilityInfo } from './FPLTypesAndProps';
/**
 * The store class for FPLDetails
 */
export declare class FPLDetailsStore {
    readonly loader: FacilityLoader;
    /** Information on our origin, arrival and destination facilities to save lookups. */
    facilityInfo: FacilityInfo;
    readonly segments: ArraySubject<FlightPlanSegment>;
    readonly activeLeg: Subject<ActiveLegDefinition>;
    readonly activeLegState: Subject<ActiveLegStates>;
    currentAltitude: number;
    selectedAltitude: number;
    /**
     * Constructor.
     * @param bus The event bus.
     */
    constructor(bus: EventBus);
}
//# sourceMappingURL=FPLDetailsStore.d.ts.map