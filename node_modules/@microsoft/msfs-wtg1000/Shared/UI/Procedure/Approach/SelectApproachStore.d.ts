import { AirportFacility, ComputedSubject, FacilityLoader, MinimumsMode, NumberUnitSubject, Subject, SubscribableArray, Unit, UnitFamily } from '@microsoft/msfs-sdk';
import { ApproachListItem, TransitionListItem } from '@microsoft/msfs-garminsdk';
import { SelectProcedureStore } from '../SelectProcedureStore';
/**
 * A data store for SelectApproach.
 */
export declare class SelectApproachStore extends SelectProcedureStore<ApproachListItem> {
    private readonly facLoader;
    readonly minimumsMode: Subject<MinimumsMode>;
    readonly minimumsUnit: ComputedSubject<Unit<UnitFamily.Distance>, string>;
    readonly decisionHeight: NumberUnitSubject<UnitFamily.Distance, import("@microsoft/msfs-sdk").SimpleUnit<UnitFamily.Distance>>;
    readonly decisionAltitude: NumberUnitSubject<UnitFamily.Distance, import("@microsoft/msfs-sdk").SimpleUnit<UnitFamily.Distance>>;
    readonly minimumsSubject: Subject<number>;
    minsToggleOptions: string[];
    readonly frequencySubject: ComputedSubject<number | undefined, string>;
    readonly selectedTransition: Subject<TransitionListItem | undefined>;
    private readonly _transitions;
    readonly transitions: SubscribableArray<TransitionListItem>;
    readonly inputValue: Subject<string>;
    readonly currentMinFeet: Subject<number>;
    /**
     * Constructor.
     * @param facLoader The facility loader.
     */
    constructor(facLoader: FacilityLoader);
    /** @inheritdoc */
    protected onSelectedFacilityChanged(facility: AirportFacility | undefined): void;
    /** @inheritdoc */
    protected onSelectedProcedureChanged(): void;
    /**
     * Refreshes the transitions array to reflect the transition list of the currently selected approach.
     */
    private refreshTransitions;
    private refreshFreqTextOpId;
    /**
     * Refreshes the approach frequency text to reflect the frequency of the currently selected approach.
     */
    private refreshApproachFrequencyText;
}
//# sourceMappingURL=SelectApproachStore.d.ts.map