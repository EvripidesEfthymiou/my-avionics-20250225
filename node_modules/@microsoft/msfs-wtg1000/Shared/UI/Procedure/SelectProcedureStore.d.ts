import { AirportFacility, ArraySubject, FlightPlan, LegDefinition, Subject, SubscribableArray } from '@microsoft/msfs-sdk';
/**
 * A data store for procedure selection components.
 */
export declare abstract class SelectProcedureStore<T> {
    readonly selectedFacility: Subject<AirportFacility | undefined>;
    protected readonly _procedures: ArraySubject<T>;
    readonly procedures: SubscribableArray<T>;
    readonly selectedProcedure: Subject<T | undefined>;
    readonly previewPlan: Subject<FlightPlan | null>;
    readonly sequence: ArraySubject<Subject<LegDefinition>>;
    /** Constructor. */
    constructor();
    /**
     * Responds to changes in the selected airport facility.
     * @param facility The selected airport facility.
     */
    protected onSelectedFacilityChanged(facility: AirportFacility | undefined): void;
    /**
     * Responds to changes in the selected procedure.
     * @param proc The selected procedure.
     */
    protected onSelectedProcedureChanged(proc: T | undefined): void;
}
//# sourceMappingURL=SelectProcedureStore.d.ts.map