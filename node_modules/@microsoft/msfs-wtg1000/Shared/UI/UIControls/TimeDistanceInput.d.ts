import { NumberUnitSubject, UnitFamily, VNode } from '@microsoft/msfs-sdk';
import { UiControlGroup, UiControlGroupProps } from '../UiControlGroup';
import './TimeDistanceInput.css';
/** Props on the TimeDistanceInput component. */
interface TimeDistanceInputProps extends UiControlGroupProps {
    /** The time value to increment or decrement with each inner knob input. */
    timeSubject: NumberUnitSubject<UnitFamily.Duration>;
    /** The distance to increment or decrement with each inner knob input. */
    distanceSubject: NumberUnitSubject<UnitFamily.Distance>;
    /** The class to apply to the control. */
    class?: string;
}
/**
 * An input that can switch between time and distance.
 */
export declare class TimeDistanceInput extends UiControlGroup<TimeDistanceInputProps> {
    private readonly distanceSubject;
    private readonly minutesSubject;
    private readonly secondsTensSubject;
    private readonly secondsOnesSubject;
    private readonly distanceInput;
    private readonly minutesInput;
    private readonly tensInput;
    private readonly onesInput;
    private readonly minutesGroup;
    private readonly distanceGroup;
    private isTimeMode;
    private ignoreUpdate;
    /** @inheritdoc */
    onAfterRender(): void;
    /**
     * Sets the input mode to time or distance.
     * @param isTime True if the input mode should be time, false otherwise.
     */
    setMode(isTime: boolean): void;
    /**
     * Updates the props subjects.
     */
    private update;
    /**
     * Renders the control.
     * @returns The rendered VNode.
     */
    render(): VNode;
}
export {};
//# sourceMappingURL=TimeDistanceInput.d.ts.map