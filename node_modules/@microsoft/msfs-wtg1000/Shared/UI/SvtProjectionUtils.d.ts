/// <reference types="@microsoft/msfs-types/js/types" />
/**
 * Projection utils for the SVT
 */
export declare class SvtProjectionUtils {
    private static readonly FOV;
    private static readonly FOCAL_LENGTH;
    /**
     * Projects vector3 to screen FoV.
     * @param x the x value
     * @param y the y value
     * @param z the z value
     * @param out The vector to which to write the results.
     * @returns The projected vector.
     */
    static project(x: number, y: number, z: number, out: Float64Array): Float64Array;
    /**
     * Relative yaw/pitch to plane, both in radians
     * @param yaw Yaw in radians
     * @param pitch Pitch in radians
     * @param roll Roll in radians
     * @param out The vector to which to write the results.
     * @returns projected vector with yaw/pitch
     */
    static projectYawPitch(yaw: number, pitch: number, roll: number, out: Float64Array): Float64Array;
    /**
     * Projects lat long alt
     * @param latLongAlt  The coordinates of the position to project.
     * @param planeCoordinates The plane position.
     * @param heading The heading of the plane in degrees.
     * @param roll Roll in radians
     * @param out The vector to which to write the results.
     * @returns The projected position.
     */
    static projectLatLongAlt(latLongAlt: LatLongAlt, planeCoordinates: LatLongAlt, heading: number, roll: number, out: Float64Array): Float64Array;
}
//# sourceMappingURL=SvtProjectionUtils.d.ts.map