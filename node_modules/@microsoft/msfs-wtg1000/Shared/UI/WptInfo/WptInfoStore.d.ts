import { Facility, FacilityWaypoint, Subject } from '@microsoft/msfs-sdk';
import { WaypointInfoStore } from '@microsoft/msfs-garminsdk';
/**
 * Wpt info store
 */
export declare class WptInfoStore extends WaypointInfoStore {
    readonly prompt: Subject<string>;
    private _matchedWaypoints;
    /** An array of waypoints which have matched the input. */
    get matchedWaypoints(): readonly FacilityWaypoint<Facility>[];
    /**
     * Set the list of matched waypoints.
     * @param waypoints An array of matched waypoints.
     */
    setMatchedWaypoints(waypoints: readonly FacilityWaypoint<Facility>[]): void;
}
//# sourceMappingURL=WptInfoStore.d.ts.map