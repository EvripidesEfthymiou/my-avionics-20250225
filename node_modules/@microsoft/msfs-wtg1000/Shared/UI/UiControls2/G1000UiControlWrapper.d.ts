import { VNode } from '@microsoft/msfs-sdk';
import { FmsHEvent } from '../FmsHEvent';
import { EntryDirection, UiControlGroup } from '../UiControlGroup';
/**
 * Wraps a G1000UiControl component to allow it to function in an original UiControl tree. If the wrapper has multiple
 * G1000UiControl descendants, all will be rendered, but only the last one (in tree order) will be functional.
 */
export declare class G1000UiControlWrapper extends UiControlGroup {
    private control?;
    /** @inheritdoc */
    onAfterRender(thisNode: VNode): void;
    /** @inheritdoc */
    getIsFocusable(): boolean;
    /** @inheritdoc */
    focus(dir?: EntryDirection): void;
    /** @inheritdoc */
    protected onBlurred(): void;
    /** @inheritdoc */
    processHEvent(evt: FmsHEvent): boolean;
    /** @inheritdoc */
    render(): VNode;
}
//# sourceMappingURL=G1000UiControlWrapper.d.ts.map