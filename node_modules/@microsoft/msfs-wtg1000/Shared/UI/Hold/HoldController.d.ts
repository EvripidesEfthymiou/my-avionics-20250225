import { Fms, GarminFacilityWaypointCache } from '@microsoft/msfs-garminsdk';
import { HoldStore } from './HoldStore';
/**
 * A controller for the Hold dialog.
 */
export declare class HoldController {
    private readonly store;
    private readonly fms;
    private readonly facWaypointCache;
    /**
     * Creates an instance of a HoldController.
     * @param store The hold store to use with this instance.
     * @param fms The FMS to use with this instance.
     * @param facWaypointCache The facility waypoint cache used by this controller.
     */
    constructor(store: HoldStore, fms: Fms, facWaypointCache: GarminFacilityWaypointCache);
    /**
     * Resets the hold dialog data.
     */
    reset(): void;
    /**
     * Accepts the currently defined hold and adds it to the flight plan.
     */
    accept(): void;
}
//# sourceMappingURL=HoldController.d.ts.map