import { NumberUnitSubject, Subject, Waypoint } from '@microsoft/msfs-sdk';
import { HoldInputData } from './Hold';
/**
 * A store for the Hold page data.
 */
export declare class HoldStore {
    /** The indexes for the hold. */
    readonly indexes: Subject<HoldInputData>;
    /** The course for the hold. */
    readonly course: Subject<number>;
    /** Whether or not the course is inbound or outbound. */
    readonly isInbound: Subject<number>;
    /** Whether or not the hold is based on time or distance. */
    readonly isTime: Subject<number>;
    /** The time that the hold legs should be. */
    readonly time: NumberUnitSubject<import("@microsoft/msfs-sdk").UnitFamily.Duration, import("@microsoft/msfs-sdk").SimpleUnit<import("@microsoft/msfs-sdk").UnitFamily.Duration>>;
    /** The distance of the hold legs. */
    readonly distance: NumberUnitSubject<import("@microsoft/msfs-sdk").UnitFamily.Distance, import("@microsoft/msfs-sdk").SimpleUnit<import("@microsoft/msfs-sdk").UnitFamily.Distance>>;
    /** The direction of the hold. */
    readonly turnDirection: Subject<number>;
    /** The current leg waypoint. */
    readonly waypoint: Subject<Waypoint | null>;
    readonly fixIcao: Subject<string>;
}
//# sourceMappingURL=HoldStore.d.ts.map